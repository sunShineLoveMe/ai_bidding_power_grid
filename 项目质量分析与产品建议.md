# AI 标书系统 - 项目质量分析与产品建议

> 分析日期：2026-04-29

---

## 一、业务完成度评分：7.5/10

### 已完成的核心链路

- 招标文件上传 -> MinerU/OCR 解析 -> 结构化落库
- AI 深度解读报告（资格核查、评分策略、风险预警）
- 标书章节大纲生成（规则版 + AI 版双模式）
- 单章节正文流式生成
- Word 导出 + ONLYOFFICE 在线编辑
- RAG 企业知识库（水利行业种子库 26 份/558 分片）
- 图片/资质资产检索
- 系统设置、历史记录、知识库管理

**链路完整度不错**，从"上传招标文件"到"生成可编辑的投标文件"已经形成闭环。

---

## 二、代码质量分析

### 2.1 后端（Python/Flask）

#### 优点

- 模块拆分清晰：`bid_interpreter.py`、`ai_chapter_planner.py`、`ai_section_writer.py`、`knowledge_retrieval.py` 各司其职
- RAG 检索链路设计合理：pgvector 相似度检索 -> 组装 Prompt -> 流式回答
- `ai_chapter_planner.py` 的"规则版 fallback + AI 版"双模式设计是亮点，AI 失败时自动降级
- 章节大纲 SSE 流式推送体验好，先出快速骨架再后台 AI 复核
- Supabase 数据层封装（`db_supabase.py`）较完整，有重试机制

#### 问题

**1. routes.py 过于臃肿（1448 行）**

所有 API 路由集中在一个文件，包含上传、解析、解读、章节、知识库、OnlyOffice 回调等全部逻辑。建议按业务域拆分 Blueprint。

**2. 双数据库混用**

`main.py` 初始化 SQLite（`bidding.db`），但核心业务走 Supabase PostgreSQL。SQLite 只存了 users、bidding、onlyoffice_documents 三张表，且 `routes.py` 中 `get_db()` 随处调用 SQLite。这种"SQLite + Supabase"混合架构会带来数据一致性隐患。

**3. 内存临时存储风险**

`temp_analysis_store = {}` 用 Python dict 在进程内传递 `pre-analysis -> chapter-analysis -> chapter-design` 的状态。如果多进程部署或服务重启，数据丢失。

**4. JSON 解析代码重复**

`_strip_llm_json` 在 `ai_chapter_planner.py` 和 `ai_interpreter.py` 中各写了一遍；`routes.py` 中还有第三套 `re.sub + json.loads` 的清理逻辑。应统一抽取为公共工具函数。

**5. 硬编码企业信息**

Prompt 中反复出现"湖北恩施清江峡能精密制造企业"、"水轮机叶片、螺母、紧固件"等描述，至少出现 6 次。如果要支持其他企业，需要全局替换。建议从配置/环境变量读取。

**6. 错误处理不统一**

有些地方 `print()` 输出错误，有些用 `logging.exception()`，有些直接 `raise`。

**7. 安全性**

- `.env` 包含真实 API Key 且已跟踪到 Git（`.gitignore` 虽然列了 `.env`，但文件实际存在）
- `ONLYOFFICE_JWT_SECRET` 有默认值 `'fsdftertrt34768586sfhjsdhfjhhjfsuhaiubue'`
- `knowledge/upload` 路由重复注册在两个 Blueprint 上

---

### 2.2 前端（React/TypeScript）

#### 优点

- TypeScript 类型定义完整（`types/interpretation.ts`、`types/bid.ts`）
- SSE 流式处理逻辑写得不错，BidEditor 中的章节大纲实时渲染体验好
- Ant Design + Tailwind 组合合理
- 目录模式和正文模式的双视图设计实用

#### 问题

**1. BidEditor/index.tsx 过于庞大（1155 行）**

章节管理、流式生成、OnlyOffice 嵌入、拖拽排序全部在一个组件里。应拆分为 hooks 和子组件。

**2. useEffect 依赖问题**

多处 `eslint-disable-next-line react-hooks/exhaustive-deps`，说明依赖数组不完整，可能引发闭包陈旧问题。

**3. 无状态管理**

虽然引入了 Zustand，但 `bidProjectStore.ts` 看起来使用有限，大部分状态在页面组件内用 `useState` 管理，跨页面数据传递靠 URL 参数。

---

## 三、架构设计评估

| 维度 | 评价 |
|------|------|
| 技术选型 | 合理，Flask + React + Supabase + pgvector 适合 MVP |
| 数据流 | 清晰：上传 -> 解析 -> 结构化 -> 解读 -> 大纲 -> 正文 -> 导出 |
| RAG 架构 | 设计规范：embedding -> pgvector RPC -> 组装 prompt -> 流式回答 |
| 可扩展性 | 中等偏低，硬编码较多，双 DB 架构限制横向扩展 |
| 部署友好度 | 一般，缺少 Docker Compose，SQLite + Supabase 混合增加运维复杂度 |

---

## 四、下一步产品建议

### P0 - 必须尽快解决

**1. 统一数据库**

将 SQLite 的三张表迁移到 Supabase PostgreSQL，消除双 DB 混用。这是后续所有功能迭代的基础。

**2. 企业信息配置化**

将"清江峡能精密制造"等企业信息、行业关键词抽取为系统配置项（可在 Settings 页面管理），Prompt 模板从配置读取。这样系统才能服务多家企业。

**3. 合规检查功能**

当前系统生成的标书没有"实质性条款覆盖检查"和"评分项完整性校验"。建议增加：

- 招标文件要求 vs 投标章节内容的覆盖度检查
- 评分项对应章节是否都有内容的自动校验
- 废标风险清单的逐项确认

### P1 - 产品竞争力提升

**4. 报价文件生成**

当前标书编制只覆盖技术部分，报价/工程量清单模块是空白。招投标中报价是核心，建议接入 Excel 解析 + 单价分析模板。

**5. 历史标书学习**

利用 `rag_seed` 中的历史标书，训练"优秀标书片段"的向量库，在生成正文时检索历史高分段落作为参考，提升生成质量。

**6. 多人协作**

当前是单用户单机模式。建议增加项目分配、章节认领、审批流等协作能力。

**7. 批量生成优化**

当前"一键编写全文"是串行逐章生成，对于 30+ 章节的标书等待时间过长。建议实现章节级别的并行生成 + 进度条。

### P2 - 体验优化

**8. 前端重构**

拆分 BidEditor 为 `useChapterManager` hook + `ChapterSidebar` + `ChapterContent` + `OnlyOfficePanel` 子组件。

**9. Prompt 模板管理**

将分散在代码中的 Prompt 抽取为模板文件，支持在 Settings 页面编辑和 A/B 测试。

**10. 生成质量评估**

增加标书正文的自动评分（如：是否回应了所有评分项、是否包含待补充占位符过多、格式是否规范），帮助用户快速识别需要人工补充的章节。

**11. 导出格式增强**

当前只支持 Word 导出。建议增加 PDF 导出、投标文件打包（含附件、资质扫描件等）。

---

## 五、总结

这是一个**完成度较高的 MVP**，核心链路已经跑通，RAG 知识库和流式生成的体验不错。最大的技术债是 SQLite + Supabase 混用和大量硬编码；最大的产品缺口是合规检查和报价模块。建议先统一数据库、配置化企业信息，再逐步补齐合规检查和报价能力。
