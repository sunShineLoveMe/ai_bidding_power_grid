# 单企业自动化标书平台 — 技术与业务架构成熟度全维度评估

> 评估日期：2026-05-09
> 评估对象：`ai_bidding_hubei`（水利行业 AI 标书系统，`product` 分支）
> 评估视角：单企业私有化部署（水利/基建总承包类企业投标小组）
> 评估方法：全量代码审查 + 架构文档复核 + 对照既有三份评审报告（`Single_Enterprise_Deployment_Maturity_Report.md` / `项目业务成熟度与功能集成评估报告.md` / `PROJECT_QUALITY_REVIEW_20260508.md`）

---

## 零、摘要与综合结论

| 项 | 结论 |
| --- | --- |
| 产品定位 | 面向水利工程建设企业的 AI 标书编制工作台，单机/私有化 MVP |
| 业务闭环 | 上传 → OCR/解析 → 结构化解读 → 分册大纲 → 流式正文 → 合规复核 → DOCX 导出 → Tiptap 编辑 —— **100% 跑通** |
| 技术底座 | Flask + React 18 + TypeScript + Supabase(PostgreSQL + pgvector) + DashScope/Qwen + MinerU + Tiptap |
| 代码规模 | 后端 ~11,200 行、前端 ~9,000 行、SQL 迁移 9 份、测试用例 9 个文件 |
| 综合成熟度 | **3.6 / 5**（MVP 完成、接近“可试点”级；距离“生产可用 SaaS”仍有半步） |
| 单企业部署适用性 | **高**：数据本地可控、行业垂直深、人机协同流畅；核心瓶颈在多人协作、备份恢复、CI/CD |

一句话定性：
> **这是一套业务侧极其完整、工程治理已进入“去 God File”攻坚期的水利行业 AI 标书 MVP。作为“标书主编型 AI 助手”在单企业私有化场景下已具备商业可用性；如果要走向多企业、公网 SaaS 或规模化交付，必须再做一轮系统化的工程加固。**

### 与既有评审报告的差异结论

- 之前 `项目质量分析与产品建议.md`（04-29）提到的“SQLite/Supabase 双写”“硬编码企业信息”“routes.py 过大”等问题，**部分已解决**（企业画像、CORS、上传校验、错误脱敏、配置中心、任务化导出/生成、重试退避均已落地），但 `backend/api/routes.py` 已从 1448 行膨胀到 **2,878 行**，`frontend/src/pages/BidEditor/index.tsx` 从 1155 行膨胀到 **2,800 行**，**God File / God Component 风险更重**，应视为 P1 级技术债。
- 之前 `Single_Enterprise_Deployment_Maturity_Report.md` 给出的“极高/可商用”是**单企业小团队内网视角下成立**的结论，但不能直接推广到公网或多企业部署。
- `PROJECT_QUALITY_REVIEW_20260508.md` 给的 85% MVP 完成度与本次评估一致。

---

## 一、业务架构评估

### 1.1 业务地图

```
                ┌──────────────────────────────────────────────────┐
                │              单企业 AI 标书工作台                 │
                └──────────────────────────────────────────────────┘
                                       │
   ┌───────────────────┬───────────────┼────────────────┬────────────────┐
   ▼                   ▼               ▼                ▼                ▼
【招标项目域】      【企业弹药库】      【AI 编排域】    【质量与合规】   【交付域】
 - 项目/文件         - 知识库 RAG       - 解读报告       - 条款响应率    - DOCX 异步导出
 - MinerU 解析       - 企业资信库       - 分册大纲       - 评分/风险     - 分册/单章拆分
 - 结构化解读        - 企业产品库       - 章节正文       - 语义复核      - Tiptap 编辑
 - 页码溯源          - 种子水利资料     - 写作计划       - 高风险提示    - OnlyOffice (可选)
                                       - 补写/扩写      - 模型追问
```

### 1.2 五步自动化工作流完成度

| 步骤 | 能力要点 | 断点续传 | 失败降级 | 成熟度 |
| --- | --- | --- | --- | --- |
| ① 上传 | 招标文件上传 + 项目/文件落库 + 扩展名/MIME 双校验 + 体积限制 | `localStorage` 恢复 | — | ★★★★★ |
| ② 解析 | MinerU 优先 + PyPDF2/Mammoth 兜底；>200 页拆分；`.part` 断点续传；DoH 解析；curl 降级；手动 ZIP 导入 | parse_id 轮询 | 多通道降级 | ★★★★☆ |
| ③ 解读 | 规则抽取 + LLM 深度解读（8 维度），项目概况/要求/评分/风险/章节建议全量结构化 | — | 规则兜底 | ★★★★☆ |
| ④ 大纲 | SSE 流式 `volumes + chapters` 分册大纲；技术/商务/资格/报价/附件/其他 6 类 | — | 规则骨架兜底 | ★★★★☆ |
| ⑤ 编制 | Tiptap 流式写作、批量并发、目录模式、合规仪表盘、补写、单章/分册导出 | 后端任务态（`bid_generation_tasks` / `bid_export_tasks`）刷新恢复 | 单章失败不污染正文 | ★★★★☆ |

结论：**五步链路 100% 贯通，断点续传与任务态设计已经从“前端内存”升级为“后端持久化任务”**，这是本次迭代相比上一次评估最显著的提升。

### 1.3 单企业场景价值映射

| 单企业真实痛点 | 系统对策 | 效果 |
| --- | --- | --- |
| 几百页招标文件人工拆解要 1–2 天 | MinerU OCR + 结构化解读 + 页码溯源 | 几分钟出资质/商务/技术/评分/风险五表 |
| 资质、业绩、证书、设备图散落多地 | 企业资信库 + 产品库（原图 + WebP 缩略图 + `applicable_volumes`） | 正文生成时按分册自动匹配 + 插图 + 可替换 |
| AI 写的通用内容偏离企业实际 | 企业画像 7 字段注入每个 Prompt | 生成内容自带企业口径、行业定位与写作约束 |
| 不同分册写作策略不同（技术重方案、商务重响应、报价禁编造） | 分册写作策略差异化（`volume_type`） | 技术/商务/资格/报价/附件分别注入不同约束与资料召回 |
| 评分项漏项、废标条款遗漏 | 规则覆盖率 + LLM 语义复核 + 下载前风险提示 | “未响应 / 高风险未响应 / 待补强”逐项抽屉 |
| 长文档导出超时、图文排版乱 | 异步导出任务 + Markdown→DOCX、宋体中文版面、图片上限与降级 | 稳定产出单企业可直接提交的 Word |

### 1.4 业务成熟度打分

| 业务维度 | 分数 | 依据 |
| --- | :---: | --- |
| 核心标书链路完整度 | 4.8/5 | 上传到导出全流程可用，且已接入语义合规与补写 |
| 行业垂直深度 | 4.5/5 | 水利种子库 26 份/558 分片、11 类产品分类、6 种分册体系 |
| 企业“弹药库”融合度 | 4.0/5 | 资信库/产品库已支持适用分册、自动插图、编辑回填 |
| AI 生成质量可控性 | 4.0/5 | 写作计划 + 目标字数分配 + `【待补充】`占位 + 补写机制 |
| 合规/风险闭环 | 4.0/5 | 规则覆盖率 + LLM 语义复核 + 下载前拦截 + 模型追问 |
| 人机协同体验 | 4.0/5 | 目录/正文双模式、Tiptap 流式、章节状态、重置保稿 |
| 多人协作与权限 | 1.5/5 | 仅浏览器指纹识别，无章节认领/锁定/审批流 |
| 成本/用量透明度 | 4.0/5 | `ai_usage_logs` 全链路记录 + 项目成本汇总 + CNY 换算 |

业务侧综合：**3.9 / 5**。短板集中在“多人协作”“工作流版本管理”“报价/工程量清单”三类扩展能力。

---

## 二、技术架构评估

### 2.1 架构分层

```
 ┌────────────────────────────── 用户浏览器 ──────────────────────────────┐
 │  Vite + React 18 + TS + AntD5 + Tailwind + Tiptap + TanStack Query    │
 │  Zustand 状态；Axios 封装；SSE 流式消费；localStorage 工作流态恢复     │
 └────────────────────────────────┬──────────────────────────────────────┘
                                  │ REST + SSE
 ┌────────────────────────────────▼──────────────────────────────────────┐
 │  Flask (main.py) + CORS 白名单 + X-App-Auth-Token/Bearer + 错误脱敏   │
 │  backend/api/routes.py          ← 业务路由与 SSE 入口（2878 行，需拆）│
 │  backend/api/users.py           ← 匿名指纹用户                         │
 ├────────────────────────────────────────────────────────────────────────┤
 │  AI 编排层 backend/ai/                                                 │
 │   ├─ interpreter.py            招标文件结构化解读                      │
 │   ├─ chapter_planner.py        双阶段分册大纲（规则+LLM，SSE 流式）    │
 │   ├─ bid_writing_plan.py       章节写作计划（字数/页数/策略）          │
 │   ├─ length_settings.py        全文篇幅 → 章节目标字数分配              │
 │   ├─ section_writer.py         流式正文 + 补写 + 企业画像注入           │
 │   ├─ compliance_checker.py     规则条款响应覆盖率                       │
 │   ├─ semantic_compliance.py    LLM 语义合规复核（证据+置信度）          │
 │   ├─ qwen_client.py            DashScope/Qwen 同步与流式，重试退避     │
 │   └─ rerank_client.py          Rerank fail-open                        │
 │                                                                        │
 │  解析层 backend/parsing/                                               │
 │   ├─ document_parser.py        原生/OCR 双通道，>200 页拆分             │
 │   ├─ mineru_client.py          MinerU v4：curl/DoH/断点续传/重试       │
 │   └─ bid_interpreter.py        规则抽取                                 │
 │                                                                        │
 │  RAG 层 backend/rag/                                                   │
 │   ├─ vector_store.py           DashScope Embedding + Chroma 兼容       │
 │   ├─ ingestion.py              图文双路入库，资产向量与关键词兜底      │
 │   └─ retrieval.py              pgvector RPC + Rerank + Prompt 组装     │
 │                                                                        │
 │  导出层 backend/export/                                                │
 │   └─ md_to_word.py             Markdown→DOCX，宋体/A4/页眉页脚/配图   │
 │                                                                        │
 │  数据访问 backend/db/ supabase_client.py + supabase_repo.py(1728 行)  │
 │  工具 backend/core/ config.py / security.py / bid_volumes.py / ...    │
 ├────────────────────────────────────────────────────────────────────────┤
 │  Supabase PostgreSQL + pgvector + Storage                              │
 │   bid_projects / bid_files / bid_analysis / bid_requirements           │
 │   bid_scoring_items / bid_risks / bid_chapter_suggestions              │
 │   bid_sections(metadata.volume_type, writing_plan)                     │
 │   knowledge_documents / document_chunks(vector 1024)                   │
 │   knowledge_assets(applicable_volumes, vector)                         │
 │   ai_model_prices / ai_usage_logs / *_summary                          │
 │   bid_generation_tasks / bid_export_tasks                              │
 │   app_users / onlyoffice_documents                                     │
 │                                                                        │
 │  SQLite（仅兼容回退）：users / bidding / onlyoffice_documents         │
 └────────────────────────────────────────────────────────────────────────┘
              │                                           │
     ┌────────▼─────────┐                  ┌──────────────▼─────────────┐
     │  DashScope/Qwen  │                  │   MinerU v4 (OCR/版面)     │
     │  qwen-turbo/long │                  │   大文件拆分 + curl fallback│
     │  text-embedding-v4                  │                            │
     │  qwen3-rerank    │                  └────────────────────────────┘
     └──────────────────┘

     可选：OnlyOffice Document Server（Docker 部署）作为终稿编辑
```

### 2.2 技术选型合理性

| 维度 | 选型 | 评价 |
| --- | --- | --- |
| 后端 | Flask 3 + 蓝图 | 轻量、上手快；但缺少 pydantic/FastAPI 的模式化校验 |
| 前端 | Vite + React 18 + TS + AntD5 + Tailwind + Tiptap | 中后台主流组合，Tiptap 满足 Markdown 双向 + 流式 |
| 状态 | TanStack Query + Zustand | Query 是亮点（缓存/重试/后台刷新），Zustand 利用率偏低 |
| 数据库 | Supabase PostgreSQL + pgvector | 单产品覆盖业务表/对象存储/向量检索/RPC，部署简单 |
| 模型 | DashScope/Qwen 全家桶（文本+向量+Rerank）+ MinerU | 国产合规、中文友好；多模型适配已抽象为 provider 字段 |
| 编辑器 | Tiptap（主）+ OnlyOffice（可选） | “纯前端 Markdown 为主 + 终稿 Office 为辅”策略正确 |
| 打包/构建 | Vite + tsc -b | 构建一致、HMR 快 |
| 部署 | `python main.py` + `npm run build` | **尚未 Docker 化 / Compose 化 / CI 化**，是单企业私有化交付最大的工程短板 |

### 2.3 代码规模与集中度（本次实测）

| 文件/区域 | 行数 | 复杂度信号 |
| --- | ---: | --- |
| `backend/api/routes.py` | **2,878** | 🔴 God File，所有业务路由、SSE、回调集中 |
| `backend/db/supabase_repo.py` | **1,728** | 🟡 数据访问全部汇总，CRUD + 聚合混杂 |
| `backend/export/md_to_word.py` | 769 | 🟢 功能聚合合理，可维护 |
| `backend/parsing/document_parser.py` | 655 | 🟢 双通道设计清晰 |
| `backend/ai/chapter_planner.py` | 641 | 🟢 规则 + AI 双阶段 |
| `backend/parsing/bid_interpreter.py` | 489 | 🟢 |
| `backend/ai/section_writer.py` | 466 | 🟡 流式 + 补写 + 降级，已不小 |
| `backend/ai/compliance_checker.py` | 395 | 🟢 纯规则 |
| `backend/parsing/mineru_client.py` | 384 | 🟢 韧性设计优秀 |
| `backend/ai/qwen_client.py` | 361 | 🟢 已接入重试/退避/超时 |
| `backend/rag/retrieval.py` | 324 | 🟢 |
| `backend/rag/vector_store.py` | 288 | 🟢 |
| `backend/core/bid_volumes.py` | 284 | 🟢 分册体系抽象 |
| `backend/core/security.py` | 214 | 🟢 CORS/鉴权/上传/脱敏集中 |
| `backend/ai/semantic_compliance.py` | 210 | 🟢 新增能力 |
| **后端合计** | **~11,200** | |

| 前端文件 | 行数 |
| --- | ---: |
| `frontend/src/pages/BidEditor/index.tsx` | **2,800** 🔴 God Component |
| `frontend/src/pages/Interpretation/index.tsx` | 793 |
| `frontend/src/pages/KnowledgeBase/KnowledgeSearchDrawer.tsx` | 519 |
| `frontend/src/pages/UsageCost/index.tsx` | 454 |
| `frontend/src/components/workflow/BidWorkflow.tsx` | 409 |
| `frontend/src/pages/QualificationBase/index.tsx` | 388 |
| `frontend/src/pages/ProductBase/index.tsx` | 386 |
| `frontend/src/components/editor/TiptapBidEditor.tsx` | 367 |
| `frontend/src/pages/History/index.tsx` | 363 |
| `frontend/src/types/interpretation.ts` | 329 |
| `frontend/src/api/bidProject.ts` | 300 |
| `frontend/src/pages/Settings/index.tsx` | 299 |
| **前端合计** | **~9,000** |

> 关键风险：`routes.py` 与 `BidEditor/index.tsx` 双双突破 2.8 千行，已远超可维护阈值。任何一次需求变更（新增分册、换模型、换鉴权模式）都会被“文件太大、冲突太多、改动风险太高”卡住。**这是本次评估最强的工程告警。**

### 2.4 集成韧性（已落地）

- DashScope：连接/读超时分离、`429/5xx` 指数退避重试、流式已出字节时**不重试**（避免重复拼接），重试次数写入 usage metadata。
- MinerU：`MINERU_DOWNLOAD_AUTO_RETRIES` + `.part` 断点续传 + curl 降级 + DoH DNS，`parse-status` 展示失败阶段/错误类型/用户提示。
- Supabase：知识库/资产索引幂等（`20260508_supabase_idempotency_indexes.sql`），批量排序、章节保存、批量生成已接入任务态表。
- DOCX 导出：异步任务（`bid_export_tasks`）+ 图片数量/体积上限 + 下载失败降级 + 图片报告 + `DOCX_ALLOW_REMOTE_IMAGES=false` 默认阻断外网下载。
- RAG：图片资产按 `applicable_volumes` 做分册硬过滤，Rerank fail-open，关键词兜底覆盖营业执照/社保/业绩/产品图。

### 2.5 安全与隐私

已完成：

- CORS 白名单（生产禁 `*`）
- `APP_LOCAL_ONLY` 本机/内网限制
- `APP_AUTH_TOKEN` + `X-App-Auth-Token` / `Authorization: Bearer`
- 生产/严格模式启动校验弱密钥（DashScope/Supabase/OnlyOffice/Auth Token）
- 500 错误默认脱敏 + 日志侧保留详情
- 上传：扩展名白名单 + MIME 校验 + `MAX_UPLOAD_MB`
- 敏感密钥掩码替换（`sanitize_exception_message`）
- 资产 `applicable_volumes` 支持按分册硬过滤，避免跨分册泄漏
- 单机部署仅使用企业内部数据；已可接入私有化 Qwen 以做到数据不出域

仍待补：

- **无显式 RBAC**：`app_users` 仅是匿名指纹。多人团队下，章节编辑、下载、导出、设置四类操作没有角色区分。
- **无审计日志**：关键动作（下载、导出、重置、删除、设置变更）没有独立审计表。
- **无速率限制**：DashScope 自身限流兜底，但 Flask 层面不存在 rate-limit 中间件。
- **SQLite 兼容层仍在**：`users` / `bidding` / `onlyoffice_documents` 三张表在 `main.py` 启动时创建，与 Supabase 主链路形成“双写兼容”，单企业部署下低风险，但长期应收敛。
- **备份恢复未实装**：Settings 页有入口，无真正的导出/回灌实现。

---

## 三、数据模型评估

13 张核心表 + 9 个 SQL 迁移脚本，已形成较完整的业务域。

| 分组 | 表 | 重要字段 |
| --- | --- | --- |
| 招标域 | `bid_projects` / `bid_files` / `bid_analysis` | `project_meta.length_settings`（字数/页数）|
| 解读域 | `bid_requirements` / `bid_scoring_items` / `bid_risks` / `bid_chapter_suggestions` | 页码/原文片段/响应建议 |
| 投标域 | `bid_sections`（4 级树） | `metadata.volume_type / volume_name / writing_plan / mapped_*` |
| 知识库 | `knowledge_documents` / `document_chunks(vector 1024)` | 分类/标签/来源/hash |
| 资产库 | `knowledge_assets` | `applicable_volumes[]` + `vector` + 结构化 specs |
| 任务态 | `bid_generation_tasks` / `bid_export_tasks` | 整批 + 单章状态、失败原因、重试次数 |
| 成本域 | `ai_model_prices` / `ai_usage_logs` / `ai_usage_project_summary` / `ai_usage_daily_summary` / `ai_usage_project_stage_summary` | provider/model/stage/token/CNY |
| 身份与编辑 | `app_users` / `onlyoffice_documents` | 浏览器指纹、document_key |

亮点：

- `bid_sections.metadata` 用 JSONB 承载 7 个数组字段，灵活承接“响应点、映射要求、映射评分、风险、材料需求、写作计划、资产引用”。
- 向量列原生存于 PostgreSQL，无需跨库 join。
- 2026-05-07/08 批量迁移已覆盖**成本统计、任务态、幂等索引、分册适用性**四类关键增强。

不足：

- JSONB 无 JSON Schema 校验，字段漂移风险取决于后端约定。
- `bid_projects` 无显式 `user_id` 外键，多人/多企业隔离靠上层业务层而非数据库约束。
- 没有 Alembic/Supabase CLI 迁移管理，多环境同步仍靠手工执行 SQL（有 ReadMe 指南，但不够自动化）。

---

## 四、AI 能力与 RAG 评估

### 4.1 AI 编排栈

```
L4 应用  解读 / 大纲 / 正文 / 补写 / 语义合规 / RAG 问答 / 模型追问
L3 编排  企业画像 + 写作计划 + 篇幅分配 + 分册策略 + 占位符规范
L2 检索  pgvector 相似度 + Rerank 重排 + 资产分册过滤 + 关键词兜底
L1 模型  qwen-turbo / qwen-long / text-embedding-v4 / qwen3-rerank / qwen-vl
L0 基座  DashScope 重试退避 / MinerU 断点续传 / Supabase 幂等
```

| 能力 | 流式 | 企业画像 | 降级 | 成熟度 |
| --- | :---: | :---: | --- | --- |
| 深度解读报告 | ✅ | ✅ | 规则兜底 | ★★★★☆ |
| 分册大纲 | ✅ SSE | ✅ | 规则骨架 | ★★★★☆ |
| 章节正文 | ✅ SSE | ✅ | 同步模式回退 | ★★★★☆ |
| 合规补写 | ✅ | ✅ | 失败保稿 | ★★★★☆ |
| 语义合规复核 | — | ✅ | 规则兜底 | ★★★★☆（新增） |
| 知识库问答 | ✅ SSE | ✅ | 非流式降级 | ★★★★★ |
| 模型追问建议 | — | — | 前端启发式 | ★★★★☆ |
| 重排（Rerank） | — | — | fail-open | ★★★★☆ |

### 4.2 RAG 关键特性

- **图文并进**：资产库图片支持向量召回 + 图片 Markdown 内联 + DOCX 正文插图。
- **分册硬过滤**：`applicable_volumes` 让资信库证照不会被技术标误召回，产品图不会被商务标误召回。
- **关键词兜底**：营业执照、社保、业绩、产品图片等短文本资产特征，避免向量语义“失焦”。
- **来源引用**：RAG 回答完成后前端展示参考资料来源。

### 4.3 RAG 短板

- **尚无混合检索**（BM25 / 关键词全文 + 向量），水利术语、设备型号、评分条款仍可能漏召回。
- **种子库体量有限**：26 份/558 分片，水库/泵站/河道治理/灌区/防汛等子类仍需企业侧持续导入历史标书。
- **章节正文引用来源未标注**：用户无法从单段正文反查“这段出自哪些招标条款或知识库资料”。
- **模型统一性**：主链路绑定 DashScope/Qwen，切换 DeepSeek / 智谱 / Moonshot / 百度千帆已有 provider 抽象但尚未经过真实切换验证。

---

## 五、前端工程与体验评估

- **路由扁平清晰**：主站 `/bidding /interpretation /knowledge /qualification /products /usage-cost /settings /history`，编辑器独立于 Layout（`/bid-editor` / `/onlyoffice-editor`），便于全屏沉浸。
- **交互亮点**：SSE 流式渲染、批量生成并发与进度、目录/正文双模式、实时合规仪表盘、资产库“编辑/替换/适用分册”、用量与成本中心可视化。
- **工程风险**：`BidEditor/index.tsx` 2800 行、25+ 个 useState、流式/批量/SSE/Tiptap/合规/下载/树操作全部耦合。
- **类型与测试**：类型定义齐全，但前端无单元/E2E 测试；部分 `any` 散落（尤其是 API 响应、元数据 JSONB 解析）。
- **全局体验问题**：部分 `window.location.href` 丢状态、局部 Loading 反馈偏弱、错误提示含技术术语（“MinerU/OCR 失败”）。
- **可访问性/国际化**：单语（zh-CN），尚无 i18n 与无障碍（AA）基线。

---

## 六、测试、运维与部署成熟度

### 6.1 测试

后端 smoke + 功能回归 9 个文件：

| 文件 | 覆盖范围 |
| --- | --- |
| `test_smoke.py` | 健康检查、上传类型、MinerU 失败状态、DOCX 图片降级 |
| `test_docx_export.py` | DOCX 标题/表格/图片/清理 |
| `test_api_sections.py` | 章节 API 校验/保存/排序/导出任务 |
| `test_mineru_status.py` | 导入失败、`Range` 续传、zip 产物识别 |
| `test_compliance.py` | 规则覆盖率/高风险/分册兼容 |
| `test_rag_retrieval.py` | 文本召回、资产召回、关键词兜底、Prompt 组装 |
| `test_rag_asset_scoring.py` | 技术/资格/商务资产匹配策略 |
| `test_length_settings.py` | 篇幅设置 → 章节字数分配 |
| `test_semantic_compliance.py` | LLM 语义复核 MVP |

优点：**从 0 到 30+ 用例，MVP 阶段的回归安全网已经建立**。
缺口：**无前端单元/E2E 测试、无覆盖率统计、无 CI**。

### 6.2 运维

- 启动：`python main.py`（Flask dev server）
- 监控：仅 `/api/health`；无 APM/Metrics/Tracing
- 日志：结构化 logging 已启用，尚无集中采集
- 任务：后端任务态落 Supabase，进程内调度；多实例时需要 Celery/Redis 队列升级

### 6.3 部署

| 维度 | 状态 | 备注 |
| --- | --- | --- |
| Docker 化 | ❌ | **最大工程短板**，交付实施完全依赖脚本化 |
| docker-compose | ❌ | Supabase/MinerU/OnlyOffice/前后端未整体编排 |
| CI/CD | ❌ | 无 GitHub Actions/Jenkins |
| 环境隔离 | 部分 | `.env` + `config/runtime_settings.json` |
| 数据迁移工具 | 部分 | 仅手动 SQL，无 Alembic/Supabase CLI |
| 备份恢复 | ❌ | 设置入口存在，未实装 |
| HTTPS/证书 | ❌ | 依赖外部反向代理 |
| 升级回滚 | 手动 | `git pull` + 重启 |

---

## 七、成本与商务视角

- **Token 成本可观测性**：`ai_usage_logs` 接入 CNY 汇率转换，按项目/阶段/模型出报表，支持单项目详情弹窗；客户演示与业务核算皆可用。
- **MinerU 成本**：当前按 0 元占位，需按页数计费真实对接；短期对客户预估影响有限，但商务合同应提示。
- **私有化可控性**：Supabase 可本地自托管，Qwen 可私有部署，MinerU 可按需替换为本地 OCR 栈，具备“数据 100% 不出域”的架构能力。
- **单企业 TCO**：单机 MVP 对企业成本最大项是 API（Qwen + MinerU）；按当前默认模型与种子规模，人均单次标书全流程（~80 页技术 + 40 页商务）费用约为个位到两位数人民币，商务可控。

---

## 八、差异化与竞争定位

| 维度 | 本系统 | 传统桌面标书软件 | 通用 AI 写作 | SaaS 标书平台 |
| --- | --- | --- | --- | --- |
| 水利行业适配 | ★★★★★ | ★★☆☆☆ | ★☆☆☆☆ | ★★★☆☆ |
| AI 深度 | ★★★★☆ RAG+流式+画像+合规 | ★☆☆☆☆ | ★★★☆☆ | ★★★☆☆ |
| 文件解析 | ★★★★☆ MinerU | ★★★☆☆ | ★☆☆☆☆ | ★★☆☆☆ |
| 合规闭环 | ★★★★☆ 规则+语义 | ★★☆☆☆ 人工 | — | ★★☆☆☆ |
| 数据本地化 | ★★★★☆ | ★★★★★ | ★★☆☆☆ | ★★☆☆☆ |
| 协作 | ★★☆☆☆ | ★★★★☆ | ★★★☆☆ | ★★★★☆ |
| 部署友好度 | ★★★☆☆ | ★★★★★ | ★★★★☆ | ★★★★☆ |

**差异化卡位**：“极懂水利、可私有部署、AI 全链路编排、合规闭环、可审计成本”的“AI 标书主编”，在单企业场景下的价值密度明显高于通用工具。

---

## 九、各维度综合评分（雷达）

| 维度 | 评分 | 环比 04-29 / 05-06 |
| --- | :---: | :---: |
| 业务流程完整度 | 4.6 / 5 | ↑ |
| 功能集成度 | 4.2 / 5 | ↑ |
| AI 能力深度 | 4.2 / 5 | ↑（新增语义合规/成本统计/分册策略）|
| 数据模型成熟度 | 3.8 / 5 | ↑（任务态/幂等索引）|
| RAG 与行业知识 | 3.8 / 5 | 持平（等待混合检索与正文引用） |
| 用户体验 | 3.6 / 5 | 持平 |
| 安全与合规 | 3.2 / 5 | ↑（CORS/鉴权/脱敏/启动校验/上传校验）|
| 测试与质量保障 | 2.8 / 5 | ↑（30+ 用例/幂等回归）|
| 运维与部署 | 2.2 / 5 | 持平 |
| 代码工程质量 | 2.6 / 5 | ↓（God File 膨胀）|
| 多人协作与权限 | 1.8 / 5 | 持平 |

**综合：3.6 / 5 — “MVP 成熟、可试点、待加固”。**

---

## 十、关键风险与机会

### 风险

1. **God File / God Component 膨胀**（routes.py 2878、BidEditor 2800）——影响所有后续迭代速度。
2. **部署工程缺失**（无 Dockerfile / Compose / CI）——单次实施人力成本高，客户接受度低。
3. **多人协作缺失**——投标组三角色（商务/技术/报价）协作边界没有系统化落位。
4. **报价/工程量清单模块空白**——标书中商务要素重头戏未覆盖。
5. **备份恢复未实装**——私有化环境一旦发生数据事故无法快速恢复。
6. **RAG 混合检索缺失**——行业术语、设备型号、评分条款仍可能漏召回。

### 机会

1. **历史标书资产化**：企业历年投中/落标的标书按章切片入库，形成企业专属“金句库”。
2. **语义合规深化**：从单条件语义复核升级为“逐评分项证据链 + 修改建议 + 版本对照”。
3. **多模型适配落地**：已抽象 provider，实际跑一轮 DeepSeek / Moonshot / 千帆切换，形成商业可选项。
4. **水利子行业扩展**：水库/泵站/河道/灌区/堤防/水闸/水电站/信息化监测 8 大子类 RAG 扩充。
5. **E2E 化**：Playwright 覆盖上传→大纲→正文→合规→下载五步，锁死主链路。

---

## 十一、路线图建议（P0–P4）

### P0：工程治理与可交付（0–2 周）

- [ ] `backend/api/routes.py` 按业务域拆分蓝图：`projects/`、`mineru/`、`interpret/`、`outline/`、`sections/`、`compliance/`、`knowledge/`、`settings/`、`export/`、`usage/`；每个蓝图控制在 ≤ 400 行。
- [ ] `frontend/src/pages/BidEditor/index.tsx` 拆为 `ChapterTree`、`DirectoryMode`、`WritingMode`、`BatchGeneratePanel`、`ComplianceDashboard`、`DownloadPanel` + hooks（`useChapterStream`, `useBatchGeneration`, `useExportTask`）。
- [ ] `backend/db/supabase_repo.py` 按领域拆分（projects/sections/knowledge/assets/usage/tasks）。
- [ ] 引入 Dockerfile（后端 + 前端 Nginx）与 `docker-compose.yml`（后端 / 前端 / MinerU 代理 / OnlyOffice 可选 / 反向代理）。
- [ ] GitHub Actions：`pytest + py_compile + npm run build + markdown lint + SQL 文件 hash` 最小 CI。

### P1：企业级交付基线（2–6 周）

- [ ] **RBAC**：角色（管理员/投标经理/商务/技术/报价/只读），章节级“认领-锁定-提交-审批”。
- [ ] **审计日志表** + UI：谁在何时做了什么（下载/导出/重置/删除/设置）。
- [ ] **备份恢复**：Supabase 数据 + 本地文件定时打包 + 一键恢复脚本。
- [ ] **速率限制**：Flask 层 rate-limit + 模型级预算（日 / 月 / 项目上限）。
- [ ] **SQLite 收敛**：`users` / `bidding` 全量迁移至 Supabase，仅保留启动兼容开关。
- [ ] **报价/工程量清单 MVP**：Excel 解析 + 分项单价模板 + 总价封面联动。

### P2：AI 深化（6–10 周）

- [ ] **混合检索**：BM25/全文 + pgvector，水利术语 stop-word 与同义词扩展。
- [ ] **章节正文引用标注**：段落级来源溯源，点击段落高亮招标条款或知识库分片。
- [ ] **语义合规升级**：逐评分项证据链 + 修改建议 + 版本差异。
- [ ] **AI 伴写**：选中润色/扩写/缩写/改写为正式腔调/生成表格。
- [ ] **长上下文管理**：章节摘要 + 相邻章节引用 + 上下文压缩。

### P3：数据飞轮（10–16 周）

- [ ] **历史标书切片入库**工具：按章切片、去敏、打分、向量化、可按“中标/落标”标签做正反样本。
- [ ] **水利子行业种子库**：8 大子类扩充到 200+ 文档级。
- [ ] **版本管理**：章节生成版本、人工编辑版本、导出版本、回滚。

### P4：SaaS 化与多企业（16+ 周）

- [ ] **多租户**：`tenant_id` 全表下沉、Storage bucket 前缀化。
- [ ] **SSO/OIDC** 接入企业侧身份。
- [ ] **可计费**：用量与成本中心升级为订阅/池化账单。

---

## 十二、最终结论

- **业务视角**：五步自动化链路完整闭环，水利行业垂直深度 + 企业画像 + 分册策略 + 合规闭环 + 成本透明 已构成鲜明差异化，**单企业私有化部署可进入试点阶段**。
- **技术视角**：底座韧性（重试/降级/断点/任务态/脱敏/校验）已经到位，AI 能力深度行业领先，但 **routes.py 与 BidEditor 的“God File/Component” 风险正在拖慢迭代速度**，必须在 P0 完成结构化重构。
- **工程视角**：测试从 0 到 30+ 已经完成第一阶段，**Docker 化、CI/CD、备份恢复、RBAC、审计日志** 这五件事不做完，无法跨过“演示版 → 交付版”的鸿沟。
- **商务视角**：成本可控、数据可本地化、模型可替换、生成内容可追溯——具备向水利/基建总承包类企业做付费试点的基础条件；建议以 **“单企业 → 集团子公司 → 子行业 SaaS”** 的三段式节奏推进。

> 本报告综合三份既有评审与本次全量代码/架构复核，建议沿用 **P0 工程治理 → P1 企业级基线 → P2 AI 深化 → P3 数据飞轮 → P4 SaaS 化** 的路线推进，六周内可完成从“MVP 试点”到“可交付私有化版本”的跃迁。

---

*本报告由 AI 基于 2026-05-09 代码与文档实测生成，关键数据来自本仓库：后端 11,200 行、前端 9,000 行、SQL 迁移 9 份、测试文件 9 个。*
