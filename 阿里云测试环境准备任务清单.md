# 电网 AI 标书系统 — 阿里云测试/生产环境准备任务清单

> 整理日期：2026-05-29  
> 项目阶段：客户合同已签，客户正在整理招标文件、历史标书、企业资信和产品资料。  
> 测试目标：先在阿里云部署可演示、可测试、可持续迭代的电网项目测试版本。  
> 一期边界：第一期无需多人协作、RBAC、审批流；允许单企业、小团队共用一个系统环境。  
> 核心原则：等待客户资料期间，开发团队优先做“不依赖客户资料但会卡上线”的工程底座，以及“电网场景最小可用”的行业适配。
> 最新状态更新时间：2026-05-31。

---

## 1. 结论摘要

当前原始清单的工程方向是对的：Docker、gunicorn、Nginx/HTTPS、PostgreSQL/OSS、任务队列、健康检查、日志，都是阿里云上线前必须认真处理的内容。

但优先级需要调整：

1. **电网业务适配不能放到上线后。**  
   客户测试的是电网项目，如果云环境搭好了但 Prompt、RAG、分册模板仍偏水利或通用工程，测试体验会打折。

2. **Celery 和 OSS 要拆成“测试版必需范围”和“生产增强范围”。**  
   测试版需要解决任务丢失和文件存储问题，但不必一开始把所有线程任务一次性重构完。

3. **客户资料未到位不是等待理由。**  
   公开国网/南网资料、电网术语库、阿里云资源、Docker 化、HTTPS、PostgreSQL 初始化、日志、安全配置都可以先做。

4. **日志体系基础版必须前置到 P0。**  
   后续 Docker、Celery、OSS、RAG 和模型流式调用都会引入新问题；先把 `request_id`、模块、阶段、业务上下文和脱敏打好，能显著降低排障成本。

5. **数据库选型已确定为 PostgreSQL（含 pgvector），Supabase 与 SQLite 退场，ChromaDB 已移除。**  
   业务数据通过 `PostgresCompatClient` 兼容层走 PostgreSQL；知识库语义检索走 pgvector 的 `match_knowledge_chunks` / `match_knowledge_assets` RPC。生产环境配 `DB_PROVIDER=postgres` 后 Supabase 云不参与。阿里云 RDS for PostgreSQL + pgvector 对当前单企业、小团队的数据规模合理，运维成本最低，无需引入独立向量库。早期遗留的 ChromaDB 已在 P1-9 统一移除，向量存储仅保留 pgvector。

### 1.1 本轮已落地的生产阻塞项清理（2026-05-30）

围绕“离生产就绪的差距”，本轮完成了三项之前清单未充分覆盖的硬阻塞项：

1. **清除 SQLite 兜底路径。** `main.py` 不再启动建 SQLite 表；`users.py`/`routes.py`/`onlyoffice.py` 移除 PostgreSQL 失败时静默回退 `bidding.db` 的逻辑，改为显式报错，杜绝用户数据写进不被备份的本地文件。基于 SQLite `bidding` 表的旧 `legacy.py` 链路（前端已无调用）统一改为返回 HTTP 410 Gone。
2. **鉴权纳入生产强制校验。** 生产模式下若未启用登录、静态令牌或仅本地访问三者之一，服务直接拒绝启动，堵住“默认裸奔”。同时修正生产启动校验：按 `DB_PROVIDER` 区分，postgres 部署要求 `DATABASE_URL` 而不再强制 Supabase 配置。
3. **seed SQL 移出 `sql/` 历史目录。** 两个 DeepSeek 价格种子迁入 `migrations/postgres/`（`003_`、`004_`），初始化脚本与 Runbook 同步更新。现在初始化所需 SQL 全部集中在 `migrations/postgres/`，`sql/` 仅留历史 Supabase 脚本。

验证：全部改动 `py_compile` 通过；完整测试套件 64 个用例通过；新增的生产访问控制校验逻辑专项验证通过。

### 1.2 向量库统一到 pgvector（2026-05-30，P1-9）

继三项生产阻塞项之后，本轮完成了向量库统一：移除 `vector_store.py` 中整套 ChromaDB（`file_to_chroma`/`query_chroma`/`init_chroma_client`）、`requirements.txt` 的 `chromadb` 依赖、`config.py` 的 `chroma_dir`、`Dockerfile.backend` 的 `chroma_db` 目录与 `docker-compose.yml` 的 `app_chroma` 卷。招标文件解析与知识库检索统一走 pgvector。核对确认 ChromaDB 写入此前是死写（检索 RPC 带 `embedding is not null` 过滤，解析 chunks 不写向量；`query_chroma` 仅有僵尸 import）。验证：卸载 `chromadb` 后应用仍正常加载，测试套件 68 个用例通过。详见 P1-9。

---

## 1.3 项目完成度与成熟度评估（2026-05-31，供决策参考）

> 本节是基于实际代码审阅的客观评估，供 CTO 决定下一步优先级使用。结论以代码为准（实际运行测试、阅读核心模块、核对入库报告），不以清单标注或口径为准。审阅范围：后端约 14000 行 Python、前端约 9800 行 TS/TSX、91 个后端 unittest（已实跑全绿）与前端生产构建。

### 1.3.1 总体结论

核心链路真实贯通、可演示、可单企业试用，**不是 PPT 工程**。主链路（上传 → MinerU 解析 → 结构化解读 → 分册大纲 → 章节正文 → 合规检查 → DOCX 导出）每一环在代码中均有真实实现。

当前阶段定位：**“测试版底座 + 行业适配已成型”**，离“生产级稳定交付”还有明确的几道坎。

参考成熟度（非精确度量，仅供判断量级）：

| 维度 | 估计成熟度 | 说明 |
| --- | --- | --- |
| 工程底座 | ~75% | Docker、gunicorn、日志、健康检查、PostgreSQL/pgvector、Redis/Celery 主链路已就位 |
| 行业适配（电网） | ~65% | Prompt/画像/知识库关键词已电网化；技术标知识库尚不完整 |
| 生产就绪度 | ~55%-60% | DOCX/解析/知识库入库已任务化；真实云资源联调、连接池、E2E 测试保护网仍是主要差距 |

### 1.3.2 清单完成度核对（实事求是）

同事更新后的清单大部分“已完成”标注属实，以下三类需要决策者区分理解：

**真实做到位（核查无误）**

- **P0-8 / P0-10 电网化**：全部 AI 模块（interpreter、chapter_planner、section_writer、qwen_client、compliance、retrieval、knowledge）中“水利工程顾问”角色已改为“电网/电力工程招投标顾问”，知识库关键词换成输变电/配网/变电站等电网术语。是实质改动，非默认值替换。
- **P0-12 / P0-13 / P1-9**：SQLite 下线、生产鉴权强制校验、ChromaDB 移除，均通过验证（卸载 chromadb 后应用仍可启动）。
- **P1-1 主链路任务化**：Celery + Redis 基础设施、DOCX 导出、招标文件解析/重试/MinerU 产物落库、知识库入库已落地；解析状态新增 `bid_parse_tasks` 表，默认写 PostgreSQL，web 与 worker 跨进程可见。
- **P1-2 健康检查**：`/ready` 真实探测 DB / Redis / 存储 / 模型配置，质量较高。
- **P0-7 OSS**：`OSSStorageBucket` 用 oss2 完整实现 upload/download/签名 URL，`oss2==2.19.1` 已在 requirements 中。代码完整，仅差真实账号联调。标注准确。

**“已完成”但有真实水分（建议管理层知晓）**

- **P0-9 电网 RAG 种子库**：入库报告显示仅入库 20 份、273 分片，**15 份 PDF 国标/规范被跳过**（含 GB 50150、GB 50168 等电气安装验收标准）。这些恰是技术标响应最关键的标准全文。清单已诚实标注“PDF 待摘要化/OCR 后补”，实质是**当前知识库对技术标的支撑尚不完整**。
- **P1-3 Alembic**：`versions/` 仅有 1 个 baseline，其 `upgrade()` 仅是把 4 个 `.sql` 文件 `exec_driver_sql` 执行一遍，`downgrade()` 直接 `NotImplementedError`。**这是“有壳无增量能力”的迁移**——后续改表结构仍需手写 SQL。标“已完成基础版”勉强成立，但不等于具备真正的迁移版本管理。

### 1.3.3 生产成熟度短板（按生产风险排序）

| 序号 | 短板 | 证据 | 风险 | 对应任务 |
| --- | --- | --- | --- | --- |
| 1 | **云上真实资源尚未联调** | 当前验证基于本地 PostgreSQL/Redis、mock OSS 与本地构建；客户阿里云 ECS/RDS/OSS/Redis 尚未提供 | 本地通过不等于云上网络、权限、RDS pgvector、OSS Endpoint、Redis broker 全部可用 | P0-1 / P0-5 / P0-6 / P0-7 / P0-11 |
| 2 | **技术标知识库关键 PDF 未入库** | RAG 入库报告显示 15 份 PDF 跳过，含 GB 50150、GB 50168、GB 50169、DL/T 5729 等关键标准 | 技术标正文和合规响应缺少标准全文支撑，容易生成泛化内容 | P0-9 / P2-1 |
| 3 | **章节正文仍是 SSE 请求内实时生成** | DOCX、招标解析、知识库入库、大纲后台精炼已迁 Celery；章节正文生成仍在 `sections/stream` 请求内实时执行 | 章节生成仍受连接、请求超时、浏览器中断影响，需通过 E2E 和失败恢复确认是否足够 | P1-7 |
| 4 | **连接池已补基础版，云上容量仍需复核** | `PostgresCompatClient`、解析状态存储、登录读写已接入进程内连接池；健康检查保留直连探测 | RDS 上线时仍需按 gunicorn worker、Celery 并发和 RDS max_connections 调整 `POSTGRES_POOL_MAX_SIZE` | P0-11 / 云上联调 |
| 5 | **集成/E2E 测试仍不足** | 已有 91 个后端 unittest，覆盖 Celery 投递、解析状态 DB、导出、RAG 等；但无真实浏览器 E2E、无真实 LLM/MinerU/OSS/RDS 全链路自动化 | 主链路云上回归仍依赖人工冒烟，客户测试前缺少自动保护网 | P1-7 / P2-6 |
| 6 | **合规检查本质仍是辅助判断** | `compliance_checker.py` 的覆盖率主要基于分词命中；`semantic_compliance.py` 的 LLM 复核失败即退回规则 | “合规覆盖率 100%”不能等同最终投标合规，交付口径必须强调人工复核 | 交付说明 / 产品口径 |
| 7 | **PostgresCompatClient 能力不完整** | RPC 仅硬编码 `match_knowledge_chunks`/`match_knowledge_assets`，其余 raise；过滤算子不全（无 or/like/lt 等） | 当前够用，是隐性扩展天花板 | 后续按需扩展 |

### 1.3.4 给不同角色的建议

**给管理层**

- 系统是真东西，能演示、能做单企业测试。但“能演示”到“能交付生产”之间还差三件硬事：**异步任务队列（否则重启丢任务）、技术标知识库补全（15 份关键国标未入库）、主链路自动化测试**。这三件不完成，贸然上客户生产环境会出可见事故。
- 2026-05-31 复核后，异步任务队列的最大短板已被明显收敛：DOCX、招标解析和知识库入库已经迁到 Celery。当前不能贸然对外宣称“生产完成”的原因，主要变成：**阿里云真实资源未联调、关键 PDF 标准未入库、E2E 与云上冒烟保护不足、RDS 连接池未做**。

**给开发团队**

- P1-1 不再是“待启动”，而是“主链路已落地，后台大纲精炼也已迁入 Celery”。下一步重点转向章节生成失败恢复、最小 E2E 和云上 worker/RDS/Redis 联调。
- Alembic 需补真实增量迁移能力，现状是空壳。
- 数据访问层连接池已补基础版；云上需按 RDS 连接数和 worker 并发调参。
- 补一条最小 E2E：上传招标文件 -> 解析完成 -> 生成大纲 -> 生成一节正文 -> 导出 DOCX。

**给产品团队**

- P0-9 的 PDF 国标入库不是小事，技术标响应质量直接依赖它，需尽快推进 PDF 摘要化 / OCR 入库。

### 1.3.5 一句话总结

底座扎实、行业适配到位、主链路贯通的**中等成熟度系统**；2026-05-31 的 Celery 提交后，长任务可靠性已从“阻塞项”降为“待云上复验与边界收口项”。距离生产交付的核心差距是**阿里云真实联调、知识库完整性、连接池和测试保护网**，而非功能缺失。清单标注大体诚实，但“已完成”在 RAG 种子库（P0-9）、Alembic（P1-3）和 P1-1 剩余边界处应理解为“基础版/主链路完成，有缺口”，不应当作全功能生产完成。

---

## 2. 优先级定义

| 优先级 | 含义 | 判断标准 |
| --- | --- | --- |
| 🔴 P0 | 测试环境上线阻塞项 | 不做就无法在阿里云稳定部署，或客户一测就暴露明显问题 |
| 🟡 P1 | 上线前强烈建议完成 | 不做也许能跑，但会显著增加故障、返工或交付风险 |
| 🟢 P2 | 测试后迭代增强 | 影响体验、效率或工程质量，但不阻塞首轮阿里云测试 |

## 2.1 状态说明

| 状态 | 含义 |
| --- | --- |
| ✅ 已完成 | 代码/文档已落地，并通过当前可执行验证 |
| 🟡 进行中 | 已开始，有部分产物；仍需补齐或联调 |
| ⬜ 待启动 | 尚未开始，条件具备后可排期 |
| ⏳ 等外部条件 | 依赖阿里云账号、客户资料、域名证书或其他外部资源 |

---

## 3. 任务总览表

| 编号 | 状态 | 优先级 | 任务 | 是否需要客户资料 | 是否需要外部支持 | 当前能否启动 | 建议负责人 | 预估 |
| --- | --- | --- | --- | --- | --- | --- | --- | ---: |
| P0-1 | ⏳ 等外部条件 | 🔴 | 阿里云资源与部署方案确认 | 否 | 需要阿里云账号/权限 | 可先整理资源清单 | 技术负责人/实施 | 0.5 天 |
| P0-2 | ✅ 已完成基础版 | 🔴 | Docker 化全栈编排 | 否 | 否 | backend/frontend/postgres/redis/celery-worker 已纳入编排 | 后端/DevOps | 2 天 |
| P0-3 | ✅ 已完成基础版 | 🔴 | gunicorn + gevent 生产启动 | 否 | 否 | backend 容器已使用 gunicorn gevent | 后端 | 0.5 天 |
| P0-4 | ✅ 已完成基础版 | 🔴 | 日志体系基础版 | 否 | 否 | 已完成代码实现，后续随模块补业务上下文 | 后端 | 1 天 |
| P0-5 | 🟡 进行中 | 🔴 | Nginx 反向代理 + HTTPS + SSE 兼容 | 否 | 域名/证书可后补 | HTTP 反代和 SSE 配置已完成，HTTPS 待云上资源 | 后端/DevOps | 1 天 |
| P0-6 | ✅ 已完成本地验证 | 🔴 | PostgreSQL 初始化流程固化 | 否 | 否 | 全新 Docker 空库已验证，RDS 待账号到位后复验 | 后端 | 1 天 |
| P0-7 | ✅ 已完成基础接口 | 🔴 | OSS 存储接入最小可用版 | 否 | 需要 OSS AccessKey/Bucket 才能真实联调 | 已完成 local/OSS 双 provider，真实 OSS 待账号 | 后端 | 2 天 |
| P0-8 | ✅ 已完成基础版 | 🔴 | 电网行业 Profile 最小可用版 | 否 | 否 | 默认 Profile/Prompt/前端展示已切到电网 | 产品/后端/AI | 2 天 |
| P0-9 | ✅ 已完成基础版 | 🔴 | 电网 RAG 种子库入库验证 | 不依赖客户私有资料 | 否 | 非 PDF 种子已入库并验证检索，PDF 待摘要化/OCR 后补 | AI/后端 | 2 天 |
| P0-10 | ✅ 已完成基础版 | 🔴 | 水利残留表述清理 | 否 | 否 | 运行路径已清理，历史文档保留迁移说明 | 后端/产品 | 1 天 |
| P0-11 | 🟡 进行中 | 🔴 | 阿里云测试环境冒烟脚本/Runbook | 否 | 否 | Gitee Runbook 已更新，阿里云专版待补 | 后端/实施 | 1 天 |
| P0-12 | ✅ 已完成 | 🔴 | SQLite 数据通道下线 | 否 | 否 | 已移除所有 SQLite 兜底路径，测试通过 | 后端 | 0.5 天 |
| P0-13 | ✅ 已完成 | 🔴 | 生产访问控制强制校验 | 否 | 否 | 生产模式无鉴权拒绝启动，已验证 | 后端 | 0.5 天 |
| P1-1 | 🟡 进行中 | 🟡 | Celery + Redis 核心任务迁移 | 否 | Redis 资源可本地先跑，云上待复验 | DOCX/解析/知识库入库主链路已落地，剩余大纲精炼等边界 | 后端 | 3 天 |
| P1-2 | ✅ 已完成基础版 | 🟡 | 健康检查 + 就绪探针 | 否 | 否 | 已完成本地实现与测试，云上部署后复验 | 后端 | 0.5 天 |
| P1-3 | ✅ 已完成基础版 | 🟡 | 数据库迁移管理 Alembic | 否 | 否 | 已完成本地迁移验证，RDS 待账号到位后复验 | 后端 | 1.5 天 |
| P1-4 | ✅ 已完成基础版 | 🟡 | 前端构建产物版本化与缓存策略 | 否 | 否 | 已完成本地构建与 Nginx 头验证，云上部署后复验 | 前端/DevOps | 0.5 天 |
| P1-5 | ✅ 已完成基础版 | 🟡 | DOCX 导出页码刷新稳定化 | 否 | 否 | backend 镜像已内置 LibreOffice，云上部署后复验 | 后端 | 1 天 |
| P1-6 | ⬜ 待启动 | 🟡 | 电网分册模板扩展 | 部分需要客户样例校准 | 否 | 可先做通用电网版 | 产品/AI/后端 | 2 天 |
| P1-7 | ⬜ 待启动 | 🟡 | 前端关键流程冒烟/E2E | 否 | 否 | 可启动 | 前端/测试 | 1.5 天 |
| P1-8 | ⬜ 待启动 | 🟡 | 阿里云 SLS 日志采集与告警 | 否 | 需要阿里云日志服务权限 | 可先设计字段 | 后端/实施 | 1 天 |
| P1-9 | ✅ 已完成 | 🟡 | 向量库统一到 pgvector、移除 ChromaDB | 否 | 否 | 已移除 ChromaDB 读写与依赖，测试通过 | 后端/AI | 2 天 |
| P2-1 | ⏳ 等客户资料 | 🟢 | 客户私有资料入库与清洗 | 需要 | 客户资料 | 暂不可完整启动 | 产品/实施/AI | 2-5 天 |
| P2-2 | ⏳ 等客户样例 | 🟢 | 国网目录编号体系 | 需要样本文档确认 | 客户样例更佳 | 可先调研 | 后端 | 1 天 |
| P2-3 | ⬜ 待启动 | 🟢 | AI 伴写能力 | 否 | 否 | 可后置 | 前端/后端/AI | 3 天 |
| P2-4 | ⬜ 待启动 | 🟢 | API 限流 | 否 | Redis | 可后置 | 后端 | 0.5 天 |
| P2-5 | ⬜ 待启动 | 🟢 | routes.py 历史残留清理 | 否 | 否 | 可后置 | 后端 | 0.5 天 |
| P2-6 | ⬜ 待启动 | 🟢 | 完整 Playwright E2E 套件 | 否 | 否 | 可后置 | 前端/测试 | 2 天 |
| P2-7 | ⬜ 待启动 | 🟢 | 前端主 chunk 拆包优化 | 否 | 否 | 可后置，P1-4 已记录构建警告 | 前端 | 0.5-1 天 |
| P2-8 | ⬜ 待启动 | 🟢 | 前端 npm audit 高危依赖排查 | 否 | 否 | 可后置，但上线前建议处理或形成风险说明 | 前端/安全 | 0.5 天 |

---

## 4. 外部条件与客户资料依赖

### 4.1 不需要客户资料，可以立即启动

这些任务开发团队现在就能干：

- P0-1 阿里云资源与部署方案确认
- P0-2 Docker 化全栈编排
- P0-3 gunicorn + gevent
- P0-4 日志体系基础版
- P0-5 Nginx/HTTPS/SSE 兼容
- P0-6 PostgreSQL 初始化流程固化
- P0-7 OSS 存储接入代码开发
- P0-8 电网行业 Profile 最小可用版
- P0-9 电网 RAG 种子库入库验证
- P0-10 水利残留表述清理
- P0-11 阿里云测试环境 Runbook
- P1-1 Celery + Redis 核心任务迁移
- P1-2 健康检查
- P1-3 Alembic
- P1-4 前端缓存策略
- P1-5 DOCX 页码刷新稳定化
- P1-7 冒烟/E2E 测试
- P1-8 SLS 日志字段和查询模板设计（云上采集需阿里云权限）

### 4.2 需要阿里云资源或账号权限

| 事项 | 所需条件 | 没有条件时如何推进 |
| --- | --- | --- |
| ECS/RDS/OSS/Redis 开通 | 阿里云账号、RAM 权限、预算确认 | 先用本地 Docker Compose 开发，输出资源规格清单 |
| HTTPS 证书 | 域名、备案/解析权限、证书申请权限 | 先用本地 Nginx HTTP 和自签证书验证 SSE |
| OSS 真实联调 | OSS Bucket、AccessKey、Endpoint | 先实现 `STORAGE_PROVIDER=oss` 代码，使用本地 `local` 回归 |
| Redis 云资源 | 阿里云 Redis 或 ECS 内 Docker Redis | 先用 `redis:7-alpine` 本地验证 |

### 4.3 需要客户提供资料后才能完整完成

| 资料 | 对应任务 | 用途 |
| --- | --- | --- |
| 真实招标文件样本 | P2-1、P2-2、P1-6 校准 | 验证解读、评分项、风险项、目录格式 |
| 历史中标标书 | P2-1、P1-6 | 学习客户真实写作风格、分册结构、常用承诺 |
| 企业资信资料 | P2-1 | 资质证书、人员证书、业绩、财务、信用资料入库 |
| 产品/设备/服务资料 | P2-1、P1-6 | 支撑技术响应、设备参数表、产品库图片/附件 |
| 客户品牌与企业画像 | P0-8、P2-1 | 生成内容避免泛化或错误企业定位 |
| 目录编号/格式要求样例 | P2-2 | 判断是否必须支持国网式编号体系 |

---

## 5. 🔴 P0 测试环境上线阻塞项

### P0-1 阿里云资源与部署方案确认

**目标**：明确测试版本部署在哪些阿里云资源上，避免开发完成后才发现网络、权限、规格不匹配。

**启动条件**：

- 不需要客户业务资料。
- 需要内部确认阿里云账号、预算、地域、网络策略。

**建议资源**：

| 资源 | 测试版建议 | 说明 |
| --- | --- | --- |
| ECS | 4C8G 起步，建议 4C16G | 跑 backend、frontend、Celery、Nginx、可选 Redis |
| RDS PostgreSQL | PostgreSQL 16，开启 pgvector 支持 | 存业务数据、任务状态、向量 |
| OSS | 私有 Bucket | 存招标文件、知识库文件、导出 DOCX、图片资产 |
| Redis | 测试版可 Docker Redis，生产建议云 Redis | Celery broker、后续限流 |
| 域名/HTTPS | 测试版可临时域名，正式版需客户域名 | Nginx 终止 SSL |

**验收标准**：

- 输出阿里云资源清单、网络端口、安全组、环境变量清单。
- 明确测试环境访问地址和负责人。

---

### P0-2 Docker 化全栈编排

**当前状态**：✅ 已完成基础版。本地 Docker Compose 已纳入 `postgres`、`redis`、`backend`、`celery-worker`、`frontend` 五类服务；backend 直连端口为 `3012`，前端 Nginx 入口为 `8080`。`celery-worker` 随 P1-1 第一批补入，复用 backend 镜像。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `Dockerfile.backend` | 后端 Python 镜像，安装依赖并以 gunicorn 启动 |
| `Dockerfile.frontend` | 前端 Node 构建 + Nginx 静态资源运行 |
| `docker/nginx/app.conf` | 前端静态资源、API 反代、SSE 代理和缓存策略 |
| `docker-compose.yml` | 编排 postgres、redis、backend、celery-worker、frontend |
| `.dockerignore` | 排除 venv、node_modules、上传文件、输出文件、本地密钥和索引目录 |
| `requirements.txt` | 增加 gunicorn/gevent 生产启动依赖 |

**技术实现方式**：

- `backend` 容器使用 `python:3.12-slim`，复制项目代码，安装 `requirements.txt`，默认执行 `gunicorn -c gunicorn.conf.py main:app`。
- `frontend` 容器使用多阶段构建：Node 负责 `npm ci && npm run build`，Nginx 负责运行 `frontend/dist`。
- Nginx 对 `/api/` 反代到 `backend:8000`，前端仍使用相对 `/api` 调用，不需要改 React 代码。
- Docker Compose 内部数据库地址改为 `postgres:5432`，对宿主机仍暴露 `15432`，便于本地脚本和调试。
- Redis 与 `celery-worker` 已纳入 compose；Celery 业务任务迁移进度详见 P1-1。
- 上传、导出、本地对象存储、备份目录使用 Docker named volumes，避免容器重建后丢失运行数据。（ChromaDB 卷已随 P1-9 移除。）

**当前验证结果**：

```bash
docker compose build backend frontend
docker compose up -d postgres redis backend celery-worker frontend
docker compose ps
curl -fsS -i http://127.0.0.1:3012/api/health
curl -fsS -i http://127.0.0.1:8080/api/health
curl -fsS -I http://127.0.0.1:8080/
```

当前本地验证结果：Docker Compose 配置静态校验通过；历史本地验证中 postgres、redis、backend、frontend 均为 `healthy`，后端健康检查、前端 Nginx 首页、Nginx 反代 `/api/health` 均返回 200。`celery-worker` 已纳入 compose，云上需与真实 Redis/RDS 一起复验。

**剩余工作**：

- 阿里云账号到位后，根据 ECS/RDS/OSS/Redis 实际地址拆分 `.env.production`。
- 如测试环境采用阿里云 RDS/Redis，需要把 compose 内部服务地址切换为云资源地址。

**现状**：当前 `docker-compose.yml` 已包含 PostgreSQL、Redis、backend、celery-worker、frontend；后续重点不是补容器入口，而是把容器内地址切换到阿里云 RDS/Redis/OSS 后复验。

**为什么是 P0**：阿里云 ECS 不应依赖手工启动进程；容器化是部署、回滚、重启、排错的基础。

**启动条件**：

- 不需要客户资料。
- 不需要阿里云真实资源，本地即可开发。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| `Dockerfile.backend` | Python 镜像、依赖安装、gunicorn 启动入口 |
| `Dockerfile.frontend` | Node 构建 + Nginx 静态资源运行 |
| `docker-compose.yml` | backend、frontend/nginx、postgres、redis |
| `.env` 注入 | 使用 `env_file`，不硬编码密钥 |
| 数据卷规划 | postgres volume、临时文件目录、日志输出 |

**验收标准**：

```bash
docker compose up -d
```

后，浏览器可访问系统首页，后端 API 正常，数据库连接正常。

---

### P0-3 gunicorn + gevent 生产启动

**当前状态**：✅ 已完成基础版。backend 容器已使用 gunicorn 23 + gevent worker 启动，默认 4 个 worker，超时时间 300 秒。【2026-05-30 更新】本地开发也统一改用 gunicorn，并写入 README 团队规范。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `gunicorn.conf.py` | 新增生产启动配置，支持环境变量覆盖 worker、timeout、worker class |
| `Dockerfile.backend` | 默认 CMD 改为 gunicorn 启动 |
| `requirements.txt` | 新增 `gunicorn==23.0.0`、`gevent==24.11.1` |
| `README.md` | 【2026-05-30】新增“后端启动方式（团队统一规范）”章节：统一用 `gunicorn -c gunicorn.conf.py main:app`，含端口差异（dev server 3012 / gunicorn 8000）、`--reload` 热重载、环境变量调参表、容器/生产说明和常见报错 |

**技术实现方式**：

- `worker_class=gevent`，为 SSE 长连接和并发请求提供更合适的 worker 模型。
- 默认 `workers=4`，适合测试环境 4C8G/4C16G 起步配置；可通过 `WEB_CONCURRENCY` 或 `GUNICORN_WORKERS` 覆盖。
- `timeout=300`、`graceful_timeout=30`，避免长章节生成或大文件处理被过早杀掉。
- gunicorn access log 默认关闭，避免重复输出非 JSON 访问日志；HTTP 请求日志由应用层结构化日志统一输出。

**第一批验证结果（DOCX 导出试点完成时）**：

```bash
docker compose exec -T backend gunicorn --version
docker compose logs --tail=25 backend
```

容器日志显示 `Starting gunicorn 23.0.0`、`Using worker: gevent`，并启动 4 个 worker。

【2026-05-30 本地验证】在项目 `.venv` 中执行 `gunicorn -c gunicorn.conf.py main:app`，日志显示 gevent worker × 4、监听 `0.0.0.0:8000`，`curl http://127.0.0.1:8000/api/health` 返回 200。

**剩余工作**：

- 后续做 SSE 专项测试时，需要同时打开 3 路以上大纲/正文/知识库流式请求验证持续输出。
- 阿里云 ECS 规格确认后，再按 CPU/内存调整 `WEB_CONCURRENCY`。

**现状**：本地开发与容器/生产统一使用 gunicorn；`python main.py`（Flask dev server）仅保留给个别需要原生调试器的临时场景。

**为什么是 P0**：系统有 SSE 流式响应，Flask dev server 不适合生产。同步 worker 在多个长连接下容易卡住。

**启动条件**：

- 不需要客户资料。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 新增依赖 | `gunicorn`、`gevent` |
| 新增配置 | `gunicorn.conf.py` |
| Docker 接入 | backend 容器默认用 gunicorn 启动 |
| SSE 验证 | 大纲、正文、知识库问答流式输出不卡顿 |

**建议配置**：

```python
bind = "0.0.0.0:8000"
worker_class = "gevent"
workers = 4
timeout = 300
graceful_timeout = 30
accesslog = None
errorlog = "-"
```

**验收标准**：

- 同时 3 路 SSE 请求不互相阻塞。
- 长章节生成不被 gunicorn 提前杀掉。

---

### P0-4 日志体系基础版

**当前状态**：✅ 已完成基础版。请求级结构化日志、`request_id` 响应头、生产异常脱敏、基础测试已落地；业务任务内部的 `project_id/file_id/task_id/section_id` 需要在后续模块改造时逐步补齐。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `backend/core/logging_config.py` | 新增统一日志配置、JSON Formatter、请求上下文、Flask 请求日志钩子 |
| `main.py` | 接入 `configure_logging()` 和 `register_request_logging(app)` |
| `backend/core/security.py` | 扩展异常脱敏，覆盖 Token、Cookie、AccessKey、DB URL 密码等常见敏感信息 |
| `tests/test_smoke.py` | 增加 `X-Request-Id` 和敏感信息脱敏测试 |

**技术实现方式**：

- 后端默认输出单行 JSON 日志到 stdout，便于 Docker、ECS 和后续阿里云 SLS 直接采集。
- 每个 HTTP 请求自动生成 `request_id`，同时写入响应头 `X-Request-Id` 和日志字段。
- 使用 `contextvars` 保存当前请求上下文，后续上传、解析、RAG、导出、Celery 任务可通过 `log_context(...)` 补充业务 ID。
- 日志字段预留 `module`、`stage`、`project_id`、`file_id`、`task_id`、`section_id`、`user_id`、`duration_ms`，先保证格式统一，再逐步接入各业务模块。
- 生产异常响应和日志摘要会统一脱敏，避免 API Key、数据库密码、OSS 密钥、Authorization、Cookie 外泄。
- 可通过 `LOG_FORMAT=json` 或 `LOG_FORMAT=text` 切换日志格式；测试/生产建议保持 JSON。

**当前验证结果**：

```bash
venv/bin/python -m py_compile main.py backend/core/logging_config.py backend/core/security.py tests/test_smoke.py
venv/bin/python -m unittest tests/test_smoke.py
venv/bin/python -m unittest discover -s tests
```

当前本地验证结果：64 个测试通过。

**剩余工作**：

- 在上传、解析、RAG、大纲、章节生成、DOCX 导出等关键函数中补充 `log_context(project_id=..., file_id=..., task_id=...)`。
- P1-8 接入阿里云 SLS 后，补齐查询模板、索引字段和告警规则。

**目标**：先把日志体系搭完整，让后续 Docker、Celery、OSS、RAG、模型调用排障都有上下文可追。这个任务不依赖客户资料，也不依赖阿里云账号，应该尽早做。

**为什么是 P0**：后续改动会涉及部署、异步任务、对象存储、向量入库和模型流式输出。没有结构化日志，bug 定位会大量依赖人工复现和贴代码；有 `request_id + module + stage + business_id` 后，排查效率会明显提升。

**启动条件**：

- 不需要客户资料。
- 不需要阿里云账号。
- 本地开发环境即可完成。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| `request_id` | 每个请求生成唯一 ID，写入响应头和所有相关日志 |
| 模块字段 | `module=auth/upload/parse/rag/outline/section/export/storage` |
| 阶段字段 | `stage=parse_pdf/embed_chunks/generate_outline/write_section/export_docx` |
| 业务上下文 | 有则记录 `project_id`、`file_id`、`task_id`、`section_id`、`user_id` |
| 耗时字段 | 关键操作记录 `duration_ms` |
| 状态字段 | `success/failed/retry/skipped` |
| JSON 日志 | 输出单行 JSON 到 stdout，后续可直接接阿里云 SLS |
| 敏感信息脱敏 | API Key、DB URL 密码、OSS 密钥、Authorization、Cookie、客户正文全文不进日志 |
| 异常摘要 | 保留异常类型、错误摘要、request_id，不在生产响应中暴露堆栈 |

**建议日志样例**：

```json
{
  "level": "INFO",
  "request_id": "req_20260529_xxx",
  "module": "rag",
  "stage": "embed_chunks",
  "project_id": "uuid",
  "file_id": "uuid",
  "duration_ms": 1820,
  "status": "success",
  "message": "knowledge chunks embedded"
}
```

**验收标准**：

- 任意一次上传、解析、大纲生成、章节生成、导出，都能按 `request_id` 串起日志。
- 出错日志包含模块、阶段、业务 ID 和脱敏后的异常摘要。
- 日志中不出现完整 API Key、数据库密码、OSS 密钥、Authorization、Cookie。
- 本地 Docker 运行时日志从 stdout 输出，方便后续被 SLS 采集。

---

### P0-5 Nginx 反向代理 + HTTPS + SSE 兼容

**当前状态**：🟡 进行中。本地 HTTP Nginx 反代和 SSE 兼容配置已随 `Dockerfile.frontend`、`docker/nginx/app.conf` 落地；HTTPS 证书、域名、阿里云安全组仍需等云资源到位后完成。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `docker/nginx/app.conf` | 前端静态资源、SPA fallback、`/api/` 反代、SSE 代理超时和缓冲配置 |
| `Dockerfile.frontend` | Nginx 运行前端构建产物 |
| `docker-compose.yml` | 对宿主机暴露 `8080:80` |

**技术实现方式**：

- `/api/` 反代到 `backend:8000`，保持前后端同域，减少浏览器跨域问题。
- `proxy_buffering off`、`proxy_read_timeout 300s`、`proxy_send_timeout 300s`，避免 SSE 被 Nginx 缓冲或提前断开。
- `index.html` 设置 `no-cache`，避免发版后浏览器继续使用旧入口文件。
- `/assets/` 使用 30 天 immutable 缓存，匹配 Vite hash 静态资源。

**剩余工作**：

- 阿里云域名、证书、安全组确认后补 HTTPS server block。
- 对实际 SSE 生成接口做并发流式输出验证。

**现状**：Flask 可托管静态文件，但生产环境需要 Nginx 负责静态资源、反向代理、HTTPS。

**为什么是 P0**：阿里云测试环境如果对外访问，HTTP 裸奔和 SSE 缓冲都会直接影响演示。

**启动条件**：

- Nginx 配置不需要客户资料。
- HTTPS 需要域名/证书；没有时可先用 HTTP 或自签证书验证。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 静态资源 | `/` 指向前端构建产物 |
| API 反代 | `/api/` 转发 backend |
| SSE 配置 | `proxy_buffering off`，`proxy_read_timeout 300s` |
| HTTPS | 阿里云证书或 Let's Encrypt |
| 缓存策略 | `index.html` 不缓存，JS/CSS hash 文件长缓存 |

**SSE 端点重点关注**：

- `/api/bidding/interpretations/{id}/bid-outline/stream`
- `/api/bidding/interpretations/{id}/sections/stream`
- `/api/knowledge/search/stream`
- `/api/bidding/interpretations/{id}/stream`

**验收标准**：

- HTTPS 可访问。
- SSE 内容逐步输出，不是等待结束后一次性显示。
- 刷新页面不会白屏。

---

### P0-6 PostgreSQL 初始化流程固化

**当前状态**：✅ 已完成本地验证。初始化脚本、Gitee 部署文档、全新 Docker PostgreSQL 空库初始化验证均已完成；阿里云 RDS 账号未到位，RDS 测试库验证后续作为外部条件到位后的复验项。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `scripts/init_postgres_schema.sh` | 新增 PostgreSQL 初始化脚本，固定执行 schema、登录表、模型价格种子数据 |
| `docs/deployment/gitee-ai-bid-runbook.md` | 更新数据库初始化说明，优先使用脚本，保留手工 SQL 顺序用于排障 |

**技术实现方式**：

- 脚本默认通过 Docker Compose 的 `postgres` 服务执行 `psql`，避免实施人员在本机安装 PostgreSQL 客户端。
- 固定 SQL 执行顺序：`001_schema.sql` -> `002_app_login.sql` -> `003_seed_deepseek_v4_flash_pricing.sql` -> `004_seed_deepseek_v4_pro_pricing.sql`。
- 【2026-05-30 更新】DeepSeek 价格 seed 已从 `sql/` 迁入 `migrations/postgres/`（重命名为 `003_`/`004_`），初始化脚本与 Runbook 路径同步更新；现在初始化所需 SQL 全部集中在 `migrations/postgres/`，不再从被标记为“历史/补丁参考”的 `sql/` 目录挑文件执行。
- 脚本执行后会检查 `bid_projects`、`bid_files`、`bid_analysis`、`bid_sections`、`knowledge_documents`、`document_chunks`、`knowledge_assets`、`app_users`、`ai_usage_logs`、`bid_generation_tasks`、`bid_export_tasks` 等核心表是否存在。
- 支持通过环境变量覆盖执行目标：`DB_SERVICE`、`POSTGRES_USER`、`POSTGRES_DB`，方便后续适配不同 Compose 服务名或云上数据库。
- 明确禁止直接整目录执行 `sql/`，该目录保留为历史脚本和补丁参考，避免 Supabase 时代脚本污染 PostgreSQL 初始化。

**当前验证结果**：

```bash
bash -n scripts/init_postgres_schema.sh
docker run --rm -d --name ai-bid-p0-6-pg-test \
  -e POSTGRES_DB=bidding \
  -e POSTGRES_USER=bidding \
  -e POSTGRES_PASSWORD=bidding_test \
  pgvector/pgvector:pg16

docker exec -i ai-bid-p0-6-pg-test psql -v ON_ERROR_STOP=1 -U bidding -d bidding < migrations/postgres/001_schema.sql
docker exec -i ai-bid-p0-6-pg-test psql -v ON_ERROR_STOP=1 -U bidding -d bidding < migrations/postgres/002_app_login.sql
docker exec -i ai-bid-p0-6-pg-test psql -v ON_ERROR_STOP=1 -U bidding -d bidding < migrations/postgres/003_seed_deepseek_v4_flash_pricing.sql
docker exec -i ai-bid-p0-6-pg-test psql -v ON_ERROR_STOP=1 -U bidding -d bidding < migrations/postgres/004_seed_deepseek_v4_pro_pricing.sql
```

验证结论：

- 全新 PostgreSQL + pgvector 空库首次初始化通过。
- 第二次重复执行同一组 migration 通过，`DeepSeek` 价格种子未重复插入。
- 核心表数量、`pgcrypto` / `vector` 扩展、`match_knowledge_chunks` / `match_knowledge_assets` RPC、DeepSeek v4 flash/pro 价格种子均验证通过。
- 临时测试容器已删除，未影响当前本地开发库。

**剩余工作**：

- 阿里云 RDS 账号到位后，在测试库执行一次受控初始化和回滚验证。
- 若后续表结构频繁变化，再将 P1-3 Alembic 提前。

**现状**：已有 `migrations/postgres/001_schema.sql` 和 `002_app_login.sql`，但文档和执行流程仍容易让实施人员误跑 `sql/` 里的 Supabase 历史脚本。

**为什么是 P0**：测试环境必须能从空库稳定初始化，不能靠人工猜执行顺序。

**启动条件**：

- 不需要客户资料。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 初始化脚本梳理 | 固定执行 `migrations/postgres/*.sql` |
| 价格种子脚本 | DeepSeek v4 flash/pro 价格 SQL 纳入初始化步骤 |
| 表结构验证 | 提供 `select table_name...` 检查命令 |
| 初始化文档 | 写入阿里云 Runbook |
| 避免误用 | 标注 `sql/` 为历史/补丁参考，不整目录执行 |

**验收标准**：

- 新空库执行一组命令后，核心表、索引、RPC 函数可用。
- 后端启动不出现 `relation does not exist`。

---

### P0-7 OSS 存储接入最小可用版

**现状**：`DB_PROVIDER=postgres` 时文件存储走本地 `storage/`。阿里云测试环境应使用 OSS 私有 Bucket。

**当前状态**：✅ 已完成基础接口。本地开发仍默认 `STORAGE_PROVIDER=local`；新增 `STORAGE_PROVIDER=oss` 时的 OSS 兼容实现，支持上传、下载、公开/签名 URL 和批量签名 URL。真实 OSS 联调需要阿里云 Bucket、Endpoint、RAM AccessKey 到位。

**为什么是 P0**：招标文件、知识库资料、DOCX 导出、图片资产不能长期依赖 ECS 本地磁盘。

**启动条件**：

- 代码开发不需要客户资料。
- 真实联调需要 OSS Bucket、Endpoint、AccessKey。

**测试版最小范围**：

| 文件类型 | 是否必须走 OSS | 说明 |
| --- | --- | --- |
| 招标文件上传 | 必须 | 客户测试第一步 |
| 知识库文件 | 必须 | 客户资料入库 |
| DOCX 导出文件 | 必须 | 下载交付物 |
| 资信/产品图片 | 必须 | 图文 RAG 和自动插图 |
| 临时解析中间文件 | 可本地临时缓存 | 处理完成后上传 OSS |

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 新增依赖 | `oss2` |
| 存储抽象 | 复用现有 Supabase Storage 兼容接口，PostgreSQL 模式下按 `STORAGE_PROVIDER` 分流到 local 或 OSS |
| OSS 实现 | `upload`、`download`、`get_public_url`、`create_signed_urls` |
| 环境变量 | `OSS_ENDPOINT`、`OSS_BUCKET`、`OSS_ACCESS_KEY_ID`、`OSS_ACCESS_KEY_SECRET`、`OSS_KEY_PREFIX`、`OSS_SIGNED_URL_EXPIRES` |
| 签名 URL | 私有 Bucket 临时访问 |

**已完成实现方式**：

| 文件/模块 | 处理结果 |
| --- | --- |
| `backend/db/postgres_compat.py` | 新增 `OSSStorageClient` / `OSSStorageBucket`，兼容 `upload`、`download`、`get_public_url`、`create_signed_urls` |
| `backend/db/supabase_client.py` | `STORAGE_PROVIDER=oss` 时 bucket 支持 `OSS_*_BUCKET` 或统一 `OSS_BUCKET` |
| `.env.example` / `docker-compose.yml` | 补齐 OSS 配置项，同时保留默认本地存储 |
| `requirements.txt` | 新增 `oss2==2.19.1` |
| `tests/test_storage_provider.py` | 覆盖 local 默认路径、OSS 前缀/签名 URL、bucket fallback |

**验收标准**：

- 本地默认 `STORAGE_PROVIDER=local` 不受影响。✅
- 设置 `STORAGE_PROVIDER=oss` 后，上传、预览、下载可通过 OSS 兼容接口执行。✅ 代码与 mock 已验证；真实 Bucket 待阿里云账号到位后联调。
- 私有 Bucket 通过签名 URL 访问，避免公开读。✅

---

### P0-8 电网行业 Profile 最小可用版

**现状**：项目已转向电网方向，但代码和历史文档中仍存在水利 Prompt、章节表达和行业默认值。

**当前状态**：✅ 已完成基础版。后端招标解读、分段解读、结果融合、章节大纲、章节正文、旧版小节生成、合规补强、知识库问答和图片资产匹配已切换为电网/电力口径；前端企业画像、知识库、产品库、资信库默认展示已切换为电网/电力示例。

**为什么是 P0**：客户测试的是电网项目，生成内容不能明显带水利口径。

**启动条件**：

- 不需要客户私有资料。
- 产品团队需要给出一期电网定位口径。

**最小范围**：

| 子项 | 说明 |
| --- | --- |
| 企业画像默认值 | 电力/电网工程、设备供货、运维检修、技术服务 |
| Prompt 角色 | 从“水利工程顾问”改为“电网/电力招投标顾问” |
| 风险识别 | 国网采购、资格审查、技术规范书响应、偏差表、交付周期 |
| 写作风格 | 严谨、合规、国网/电力工程表达 |
| 禁止编造 | 资质、人员、业绩、型号、检测报告不得凭空生成 |

**已完成实现方式**：

| 文件/模块 | 处理结果 |
| --- | --- |
| `backend/ai/interpreter.py` | 招标解读角色改为电网/电力工程招投标顾问 |
| `backend/ai/chapter_planner.py` | RAG 查询前缀和章节规划角色改为电网工程投标 |
| `backend/ai/section_writer.py`、`backend/ai/qwen_client.py` | 正文生成默认行业改为电网/电力工程、设备供货、安装调试、运维检修 |
| `backend/api/knowledge.py`、`backend/rag/retrieval.py` | 知识库问答相关性关键词、兜底答复和追问建议改为电网/电力范围 |
| `backend/api/routes.py`、`backend/api/assets.py` | 图片资产行业默认值和配图匹配关键词改为输变电、配网、变电站、开关柜、电缆、运维检修等 |
| `frontend/src/pages/*` | 产品库分类、知识库分类、问答引导、企业画像占位、资信示例改为电网/电力 |

**验收标准**：

- 上传电网公开样例后，解读和正文不再出现明显水利术语。
- 章节建议能覆盖技术规范书响应、商务响应、资格文件、供货/施工/运维服务等场景。

---

### P0-9 电网 RAG 种子库入库验证

**现状校正**：`rag_seed/power_grid_resources/` 已有初版国网电力行业 RAG 种子库，不是空目录。当前需要做的是清洗、补充、入库和检索验证。

**当前状态**：✅ 已完成基础版。已新增电网种子库入库脚本，并完成本地 PostgreSQL/pgvector 入库验证；默认入库策略跳过 PDF，避免标准/法规 PDF 粗切导致噪声和版权边界问题，后续 PDF 资料应走 MinerU/OCR + 摘要化后再补充。

**为什么是 P0**：客户资料未到位前，公开资料和自建标准话术就是测试版质量的基本盘。

**启动条件**：

- 不需要客户私有资料。
- 需要 DashScope Key 做 embedding。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 种子库清洗 | 排除下载失败 URL、版权风险全文 |
| 入库脚本适配 | 参考水利入库脚本，形成电网入库脚本 |
| 分类体系 | tender_documents、policy_regulations、standards_specs、standard_phrases |
| 检索验证 | 问答能召回国网采购、输变电、配网、安全质量等资料 |
| 引用边界 | 对国标/企标全文保持版权边界，优先二级知识 |

**已完成实现方式**：

| 文件/模块 | 处理结果 |
| --- | --- |
| `rag_seed/power_grid_resources/_scripts/ingest_power_grid_rag_seed.py` | 新增电网 RAG 入库脚本，支持 `--dry-run`、`--refresh`、`--category`、`--limit`、`--include-pdf` |
| `rag_seed/power_grid_resources/ingestion_report.md` / `.json` | 记录本次入库结果：41 条候选，26 条文档已入库或已存在，279 条向量分片，15 条 PDF 默认跳过 |
| `frontend/src/pages/KnowledgeBase/index.tsx` | 新增 `power_grid_*` 分类中文标签，前端可正确展示电网分类 |
| `docs/deployment/gitee-ai-bid-runbook.md` | 补充电网种子库入库命令、验证 SQL、检索验证命令和 PDF 边界说明 |

**本地验证结果**：

```bash
venv/bin/python rag_seed/power_grid_resources/_scripts/ingest_power_grid_rag_seed.py --dry-run --category 04_standard_phrases
venv/bin/python rag_seed/power_grid_resources/_scripts/ingest_power_grid_rag_seed.py --category 04_standard_phrases
venv/bin/python rag_seed/power_grid_resources/_scripts/ingest_power_grid_rag_seed.py
```

- 已入库电网文档：26 条。
- 已入库电网向量分片：279 条。
- 分布：`power_grid_policy_regulations` 11 条、`power_grid_standard_phrases` 6 条、`power_grid_standards_specs` 7 条、`power_grid_tender_documents` 2 条。
- 典型问题“电网设备采购投标文件资格审查和技术规范书响应需要注意什么”可召回国网采购公告和电网章节库资料。
- 【2026-05-30 更新】Rerank 已修复并完成真实 smoke：`qwen3-rerank` 改走百炼新版 `compatible-api/v1/reranks` 接口，旧版 `gte-rerank-v2` 继续兼容 `text-rerank` 接口；默认模型可返回重排分数。

**验收标准**：

- `knowledge_documents`、`document_chunks` 中有电网分类数据。
- 典型问题能召回电网资料，不再主要召回水利资料。

---

### P0-10 水利残留表述清理

**目标**：清理用户可见、模型可感知、输出会污染电网测试效果的水利残留。

**当前状态**：✅ 已完成基础版。运行代码和前端可见路径已不再保留水利默认行业表达；历史研究报告、旧合同、历史水利素材和迁移说明保留，避免破坏项目演进记录。

**启动条件**：

- 不需要客户资料。

**工作内容**：

| 范围 | 说明 |
| --- | --- |
| Prompt | `backend/ai/*` 中“水利工程”角色和话术 |
| 默认行业 | `knowledge_assets.industry`、企业画像默认值 |
| 前端文案 | 系统设置、示例说明、空状态文字 |
| 文档 | README/部署文档中区分历史水利和当前电网 |
| 测试样例 | 保留水利样本但标为历史/通用解析测试，不作为当前行业默认 |

**已完成实现方式**：

- 使用 `rg` 扫描 `backend/`、`frontend/`、当前部署 Runbook 和任务清单中的 `水利`、`ChromaDB`、`bidding.db`、`chroma_db`、`Supabase PGVector` 等残留词。
- 用户可见和模型可感知的水利默认 Prompt、前端空状态、默认分类、资产行业字段已替换为电网/电力口径。
- `docs/deployment/gitee-ai-bid-runbook.md` 已将“导入水利基础数据”改为“导入行业基础数据”，明确历史 `rag_seed/water_*` 仅作迁移参考，当前电网 RAG 入库由 P0-9 承接。
- 运行代码里仅保留 pgvector 迁移相关历史注释中的 `ChromaDB` 字样，不影响用户界面和业务输出。

**验收标准**：

- 客户测试链路中看不到明显“水利工程”默认表达。
- 水利种子库只作为迁移/测试参考，不参与默认电网知识库。

---

### P0-11 阿里云测试环境冒烟脚本/Runbook

**当前状态**：🟡 进行中。Gitee 首次交付/本地启动 Runbook 已补充 PostgreSQL 初始化脚本说明；阿里云专版仍需等 ECS/RDS/OSS/Redis 资源信息确认后补齐。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `docs/deployment/gitee-ai-bid-runbook.md` | 更新 Gitee 拉取、环境配置、PostgreSQL 初始化和故障处理说明 |
| `scripts/init_postgres_schema.sh` | Runbook 中引用的数据库初始化入口 |

**技术实现方式**：

- 先把“从 Gitee 拉代码 -> 配置环境变量 -> 初始化 PostgreSQL -> 启动服务 -> 健康检查”的本地/服务器通用链路固化。
- 数据库初始化统一调用脚本，避免文档里散落多条 SQL 导致实施人员漏执行或顺序错误。
- 阿里云账号未到位前，Runbook 先覆盖本地 Docker 和通用 Linux 服务器路径；等资源到位后补充 ECS 安全组、RDS 白名单、OSS Endpoint、SLS 采集等云上细节。

**当前验证结果**：

- 文档已更新到可指导 Gitee 首次拉取和 PostgreSQL 初始化。
- 阿里云真实环境尚未验证，状态不能标记为完成。

**剩余工作**：

- 阿里云资源到位后补充 `.env.production` 示例字段、端口、安全组、OSS/RDS/Redis 联通检查。
- 增加一键冒烟脚本或半自动检查清单，覆盖登录、上传、解析、大纲、正文、导出。

**目标**：让实施和开发能用同一套步骤部署、验证、定位问题。

**启动条件**：

- 不需要客户资料。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 部署步骤 | 拉代码、配置 `.env.production`、启动容器、初始化 DB |
| 检查命令 | DB 表、pgvector、OSS、Redis、健康检查 |
| 冒烟流程 | 登录、上传、解读、大纲、正文、导出 |
| 故障处理 | 常见错误和处理方法 |
| 回滚方案 | 镜像版本、数据库备份、配置恢复 |

**验收标准**：

- 新同事按文档可独立部署测试环境。
- 每次上线后有固定冒烟检查。

---

### P0-12 SQLite 数据通道下线

**当前状态**：✅ 已完成（2026-05-30）。

**背景**：项目数据库已切换到 PostgreSQL（`DB_PROVIDER=postgres` 时通过 `PostgresCompatClient` 兼容层访问），但代码中仍残留 SQLite 路径：`main.py` 启动时无条件建 `bidding.db` 表；`users.py` 在 PostgreSQL 失败时静默回退写 SQLite（`storage: sqlite_fallback`）；`routes.py`/`onlyoffice.py` 的 ONLYOFFICE 文档映射也有 SQLite 回退；旧 `legacy.py` 链路直接把 SQLite `bidding` 表当主存储。

**为什么是 P0**：生产环境若 PostgreSQL 出问题，用户数据会被静默写进一个不被备份、ECS 重建即丢的本地 SQLite 文件，属于数据一致性与可恢复性的硬隐患。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `main.py` | 移除 `init_db()` SQLite 建表逻辑和 `import sqlite3` |
| `backend/api/users.py` | `/identify` 移除 `sqlite_fallback`，PostgreSQL 失败时显式报错；移除 `get_db()`；`storage` 标识改为 `postgres` |
| `backend/api/routes.py` | `save_onlyoffice_document_mapping` 移除 SQLite 回退；`get_db()`/`read_tender_file()` 改为抛错占位；移除 `import sqlite3` |
| `backend/api/onlyoffice.py` | `save_callback` 移除文档映射 SQLite 回退查询和 legacy `bidding` 表写入；移除 `get_db` 导入 |
| `backend/api/legacy.py` | 整模块旧链路（前端已无调用）改为返回 HTTP 410 Gone + `deprecated: true`，不再连任何存储 |
| `backend/ai/qwen_client.py` | 移除未使用的 `import sqlite3` |
| `backend/core/config.py` | 移除 `sqlite_db` 默认项和 `SQLITE_DB_PATH` 环境变量映射 |

**当前验证结果**：

- 全部改动 `py_compile` 通过。
- 完整测试套件 64 个用例通过。
- legacy 四个停用路由确认返回 410。

**剩余工作**：

- 根目录本地遗留的 `bidding.db`（未被 git 跟踪，已在 `.gitignore`）已不再被任何代码读写，可由各自本地环境自行删除。
- `routes.py` 的 `save_bid_section`/`merge_sections` 现为死代码（仅旧 legacy 曾用，不涉及 SQLite），归入 P2-5 路由清理。

---

### P0-13 生产访问控制强制校验

**当前状态**：✅ 已完成（2026-05-30）。

**背景**：`enforce_request_guard` 默认 `APP_LOGIN_ENABLED`、`APP_AUTH_ENABLED` 均为 false，此时所有 `/api/` 请求被直接放行。原 `validate_startup_security` 不校验“是否启用了任一访问控制”，导致生产环境可能在公网上裸奔。

**为什么是 P0**：阿里云一旦对公网开放，无鉴权等于任何人可调用上传、解析、生成、导出接口，属于上线前必须堵住的安全阻塞项。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `backend/core/security.py` | `validate_startup_security()` 在生产模式下，若 `APP_LOGIN_ENABLED` / `APP_AUTH_ENABLED` / `APP_LOCAL_ONLY` 三者都未开启则拒绝启动；并按 `DB_PROVIDER` 区分必需配置，postgres 部署要求 `DATABASE_URL` 而不再强制 Supabase 配置 |

**当前验证结果**（专项脚本验证）：

- 生产模式 + 无任何访问控制 → 启动抛错（拒绝裸奔）。
- 生产模式 + 开启登录 + 强 `APP_SESSION_SECRET` → 通过校验。
- 生产 postgres 部署但缺 `DATABASE_URL` → 报缺失配置。

**剩余工作**：

- 部署时由实施在 `.env.production` 中明确选择并配置一种访问控制方式（推荐 `APP_LOGIN_ENABLED=true` + 强 `APP_SESSION_SECRET`）。
- API 限流（P2-4）在公网开放或多人测试时建议同步提前。

---

## 6. 🟡 P1 上线前强烈建议完成

### P1-1 Celery + Redis 核心任务迁移

**调整说明**：原清单把 Celery 全量迁移列为 P0。这里调整为 P1，但要求优先迁移最容易影响测试稳定性的任务。

**当前状态**：✅ 主链路已完成，云上待复验。Celery + Redis 基础设施、DOCX 异步导出、MinerU 招标解析/入库/重试/产物落库、知识库入库、大纲后台精炼均已迁移到 Celery，解析状态已从本地文件迁到 PostgreSQL（跨进程可见）。章节正文仍采用 SSE 实时生成，不纳入本任务范围。

**本次已落地产物（第一批：基础设施 + DOCX 导出试点）**：

| 文件 | 说明 |
| --- | --- |
| `requirements.txt` | 新增 `celery==5.4.0`、`redis==5.0.8` |
| `backend/tasks/celery_app.py` | 新增 Celery 应用：broker/backend 走 `REDIS_URL`；`task_acks_late` + `task_reject_on_worker_lost` 保证 worker 被杀后任务重投递；`FlaskTask` 基类在任务执行时推入 Flask 应用上下文；`CELERY_TASK_ALWAYS_EAGER=true` 切同步执行供测试 |
| `backend/tasks/export_tasks.py` | DOCX 导出任务体从裸线程迁入 Celery 任务 `bid.export.docx`，状态仍写 `bid_export_tasks` |
| `backend/api/export.py` | `download-docx` 路由改为 `run_bid_docx_export.delay(...)` 投递，移除 `threading.Thread`，响应契约不变 |
| `backend/api/health.py` | `/ready` 新增 `celery` 检查项（control ping 探测在线 worker） |
| `docker-compose.yml` | 新增 `celery-worker` 服务，复用 backend 镜像，以 `celery -A backend.tasks.celery_app:celery_app worker` 启动 |
| `.env.example` | 补充 `REDIS_URL`（含本地裸跑连 16379 说明）、`CELERY_WORKER_CONCURRENCY`、`CELERY_TASK_ALWAYS_EAGER` |
| `tests/test_celery_export_tasks.py` | 新增 7 个集成冒烟测试：投递契约、成功写 completed、失败写 failed、查询 404/200 |

**当前验证结果**：

```bash
.venv/bin/python -m unittest tests.test_celery_export_tasks   # 7 通过
.venv/bin/python -m unittest discover -s tests                 # 85 通过
# 真实 worker 冒烟（本地 redis:7-alpine，端口 16379）
.venv/bin/celery -A backend.tasks.celery_app:celery_app worker --loglevel=info --pool=solo
```

- 全套测试 85 个通过（迁移前 78，新增 7 个 Celery 集成冒烟）。
- 非 eager 模式下确认 `bid.export.docx` 已注册、`task_acks_late`/`task_reject_on_worker_lost` 均为 True。
- 真实 worker 连本地 Redis 启动成功：`Connected to redis://127.0.0.1:16379/0`、任务注册、`ready`。

**启动条件**：

- 不需要客户资料。
- 本地 Redis 可先开发，阿里云 Redis 后联调。

**优先迁移顺序**：

| 顺序 | 任务 | 状态 | 原因 |
| --- | --- | --- | --- |
| 1 | DOCX 异步导出 | ✅ 已迁移 | 长任务，失败影响交付物；状态已在 `bid_export_tasks`，风险最低，先立基础设施 |
| 2 | MinerU 解析/入库 | ✅ 已迁移 | 文件解析耗时长，容易因进程重启丢失；解析状态已从本地文件 `mineru_status.json` 迁到 DB（`bid_parse_tasks`） |
| 3 | 知识库入库 | ✅ 已迁移 | 与解析共用一套状态迁库与 Celery 方案 |
| 4 | 大纲后台精炼 | ✅ 已迁移 | `_refine_bid_outline_in_background` 已由 `bid.outline.refine` Celery 任务执行，完成后写回 DB |

> 说明：原清单"批量章节生成"经代码核对**不是后端线程任务**——它是前端逐章调 SSE（`sections/stream`）、后端只通过 `section-generation-tasks` 接口记录状态，因此**不纳入 Celery 迁移范围**。SSE 流式接口（大纲/正文/知识库问答）同理排除。

**剩余工作**：

- 云上部署后，用真实 RDS/Redis 复验 worker 与 web 跨进程解析状态一致性。
- 章节正文 SSE 生成保持实时模式，需通过 P1-7 最小 E2E 和失败恢复策略验证是否满足测试环境。

**本次已落地产物（第二批：MinerU 解析链路 + 解析状态迁 DB）**：

| 文件 | 说明 |
| --- | --- |
| `migrations/postgres/005_bid_parse_tasks.sql` | 新增 `bid_parse_tasks` 表（parse_id 为 text，status_payload 为 jsonb），按 supabase_file_id / parse_status 建表达式索引 |
| `backend/parsing/parse_status_store.py` | 解析状态双后端存储：`db`（PostgreSQL，JSONB `\|\|` 原子浅合并，跨进程可见）/ `file`（本地 JSON，调试用），由 `PARSE_STATUS_BACKEND` 选择，默认 db；新增 `find_parse_status_by_supabase_file` 替代文件 glob |
| `backend/parsing/document_parser.py` | `write_parse_status`/`read_parse_status` 改为薄委托新 store，签名不变，避免改动 ~20 个调用点 |
| `backend/tasks/parse_tasks.py` | 新增 5 个 Celery 任务：招标同步解析、解析入库、下载重试、产物落库、知识库入库 |
| `backend/api/projects.py` | 上传、重试解析改为 `.delay()` 投递；`_find_local_parse_status_for_supabase_file` 改走 DB 查询；移除裸线程 |
| `backend/api/mineru.py` | `get_parse_status` 内 3 处恢复线程（重解析/下载重试/产物落库）+ ingest 接口改为 Celery 投递；移除裸线程 |
| `backend/api/knowledge.py` | 知识库上传改为 Celery 投递；移除裸线程 |
| `backend/tasks/celery_app.py` | 注册 parse_tasks 模块；`FlaskTask` 改为 importlib 按绝对路径兜底加载 main，保证 worker 跨 CWD/容器稳定推入应用上下文 |
| `docker-compose.yml` | backend 与 celery-worker 新增共享卷 `app_parsed_outputs`，保证解析产物跨进程可见 |
| `scripts/init_postgres_schema.sh` | 初始化纳入 005 迁移，核心表校验加入 `bid_parse_tasks` |
| `.env.example` | 新增 `PARSE_STATUS_BACKEND` 说明 |
| `tests/test_celery_parse_tasks.py` | 新增 6 个集成测试：5 个任务注册、上传/重试/知识库投递契约、DB store 合并/查找/非 uuid 分片键 |

**本次已落地产物（第三批：大纲后台精炼 + 连接池）**：

| 文件 | 说明 |
| --- | --- |
| `backend/tasks/outline_tasks.py` | 新增 `bid.outline.refine` Celery 任务，承接原大纲后台精炼逻辑 |
| `backend/ai/chapter_planner.py` | `stream_bid_outline` 不再启动 daemon thread，改为投递 `refine_bid_outline.delay(...)` |
| `backend/db/postgres_pool.py` | 新增轻量 PostgreSQL 连接池，默认启用，支持 `POSTGRES_POOL_MAX_SIZE` / `POSTGRES_POOL_TIMEOUT_SECONDS` |
| `backend/db/postgres_compat.py` | `PostgresCompatClient.connection()` 接入连接池 |
| `backend/parsing/parse_status_store.py` | 解析状态 DB 后端接入连接池 |
| `backend/api/users.py` | 登录用户查询/创建/登录时间更新接入连接池 |
| `.env.example` / `docker-compose.yml` | 新增连接池配置项并注入 backend/celery-worker |
| `tests/test_postgres_pool.py` / `tests/test_chapter_planner.py` | 覆盖连接复用、禁用连接池、大纲精炼 Celery 投递 |

**第二批验证结果**：

```bash
# 应用迁移
docker exec -i ai-bidding-postgres psql -U bidding -d bidding < migrations/postgres/005_bid_parse_tasks.sql
# 全套测试（DATABASE_URL 指向本地 PG，DB store 用例参与）
DATABASE_URL=... .venv/bin/python -m unittest discover -s tests   # 91 通过
# 真实 worker + 跨进程投递：独立进程投递 → worker 执行 → 状态写回 DB → 投递方从 DB 读到
```

- 全套测试 91 个通过（第一批后 85 + 第二批新增 6）。
- 真实 worker 注册全部 6 个任务（1 导出 + 5 解析/知识库）、连 Redis、ready。
- 跨进程冒烟：独立进程投递 `ingest_artifacts`，worker 进程执行并把 `supabase_ingest_status` 写回 DB，投递方从 DB 读到 —— 证明 web 与 worker 跨进程解析状态可见（第二批核心目标）。
- 修复一处 worker 启动隐患：worker 不以项目根为 CWD 时 `from main import app` 会 ModuleNotFoundError，已改为 importlib 绝对路径兜底加载。

**2026-05-31 复核验证**：

```bash
.venv/bin/python -m unittest discover -s tests   # 94 通过
npm run build                                    # 通过，仍有主 chunk 体积警告
docker compose config --quiet                    # 通过
```

- 后端 94 个 unittest 全绿，覆盖 Celery 导出、Celery 解析投递、Celery 大纲精炼投递、解析状态 DB store、连接池、RAG、DOCX、健康检查等。
- 前端生产构建通过；仍存在 `dist/assets/index-*.js` 约 2.55 MB、gzip 约 760 KB 的 chunk 警告，对应 P2-7，暂不阻塞测试环境。
- 本轮未对真实阿里云 RDS/Redis/OSS/MinerU 做联调，因为客户/阿里云测试资源尚未到位。

**验收标准**：

- kill Celery worker 后任务有失败状态或可重试，不再无痕消失。✅（第一批失败可见性 + 第二批状态入 DB）
- 前端轮询 API 契约不变。✅（upload/retry-parse/parse-status/knowledge 契约测试通过）
- 解析状态跨进程可见。✅（第二批真实跨进程冒烟通过）

---

### P1-2 健康检查 + 启动就绪探针

**状态**：✅ 已完成基础版。已新增 `/api/health` 与 `/api/ready`，并补充登录开启时部署探针可访问的测试覆盖；阿里云测试环境部署后需用真实 RDS/Redis/OSS 配置复验。

**启动条件**：不需要客户资料。

**工作内容**：

| 端点 | 检查内容 |
| --- | --- |
| `/api/health` | 服务进程存活 |
| `/api/ready` | PostgreSQL、Redis、存储 provider、本地模型 Key 配置检查 |
| Docker HEALTHCHECK | backend 容器健康状态，继续使用轻量 `/api/health`，避免依赖短暂抖动导致容器反复重启 |

**技术实现方式**：

- 后端新增 `backend/api/health.py`，统一承载健康检查 Blueprint。
- `/api/health` 为轻量存活探针，只判断 Flask/gunicorn 进程是否可响应。
- `/api/ready` 为就绪探针，检查 `DATABASE_URL`、pgvector、知识库匹配函数、`REDIS_URL`、本地/OSS 存储配置、DeepSeek/DashScope/MinerU Key 配置。
- `/api/ready` 在 `APP_LOGIN_ENABLED=true` 时仍允许部署系统直接访问，避免 Nginx、Docker、阿里云健康检查被登录拦截。
- 当前 OSS ready 检查只校验 OSS provider 配置和 bucket client 构造；真实 OSS 写读冒烟需要客户提供 AccessKey、Bucket、Endpoint 后执行。

**本地验证命令**：

```bash
venv/bin/python -m unittest tests.test_smoke
venv/bin/python -m unittest discover -s tests
docker compose config >/tmp/ai_bid_compose_check.yml
curl -i http://127.0.0.1:3012/api/health
curl -i http://127.0.0.1:3012/api/ready
```

**验收标准**：

- 依赖异常时 ready 返回明确失败项。
- Nginx/部署脚本可用健康检查判断服务是否可接流量。
- 登录开启时 `/api/ready` 不返回 401，便于阿里云测试环境探针直接调用。

---

### P1-3 数据库迁移管理 Alembic

**状态**：✅ 已完成基础版。已新增 Alembic 配置、baseline revision 和迁移执行脚本；当前本地 PostgreSQL 已升级到 `20260530_0001 (head)`。阿里云 RDS 测试库需等账号到位后执行同一迁移命令复验。

**启动条件**：不需要客户资料。

**说明**：保留 `migrations/postgres/*.sql` 作为当前 PostgreSQL schema 的权威 SQL；Alembic baseline revision 复用这些 SQL 并记录版本，避免手工重复维护两套 schema。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| 初始化 Alembic | 新增 `alembic.ini`、`migrations/alembic/env.py`、revision 模板 |
| baseline migration | `20260530_0001_baseline_postgres_schema.py` 固定执行 `001_schema.sql` 至 `004_seed_*.sql` |
| 部署集成 | 新增 `scripts/migrate_postgres.sh`，默认连本地 Docker PostgreSQL；云上通过 `DATABASE_URL` 覆盖 |
| 依赖补充 | `requirements.txt` 增加 `alembic==1.16.1` |
| 历史 SQL 标注 | `sql/` 继续作为历史/补丁参考，不作为初始化入口 |

**技术实现方式**：

- `DATABASE_URL=postgresql://...` 在 Alembic env 中自动适配为 SQLAlchemy 的 `postgresql+psycopg://...`，继续使用项目现有 `psycopg` v3 依赖，不额外引入 `psycopg2`。
- baseline revision 使用 `exec_driver_sql()` 执行原始 SQL，避免 SQLAlchemy 把 JSON 字符串中的冒号误识别为绑定参数。
- `downgrade()` 不支持自动回滚。测试环境回滚按 Runbook 先备份/快照，再使用数据库恢复；生产变更另写独立可审查 revision。

**本地验证命令**：

```bash
venv/bin/pip install -r requirements.txt
scripts/migrate_postgres.sh
DATABASE_URL=postgresql://bidding:bidding_local_dev@127.0.0.1:15432/bidding venv/bin/alembic current
```

**本地验证结果**：

- `scripts/migrate_postgres.sh` 执行成功。
- `alembic current` 返回 `20260530_0001 (head)`。
- 当前本地 PostgreSQL 已存在 `alembic_version` 表，版本号为 `20260530_0001`。

**验收标准**：

- 新空库执行 Alembic 后可运行系统。
- 已有本地库重复执行 `scripts/migrate_postgres.sh` 不重复建表、不重复插入 DeepSeek 价格种子。
- 阿里云 RDS 账号到位后，使用真实 RDS `DATABASE_URL` 执行并确认 `/api/ready` 通过。

---

### P1-4 前端构建产物版本化与缓存策略

**状态**：✅ 已完成基础版。已新增构建版本文件生成、浏览器控制台版本日志、Nginx 缓存策略强化；阿里云测试环境部署后需在公网域名下复验响应头。

**启动条件**：不需要客户资料。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| `index.html` | Nginx 返回 `Cache-Control: no-cache, no-store, must-revalidate`，避免发版后继续加载旧入口 |
| JS/CSS hash 文件 | `/assets/` 返回 1 年 immutable 缓存，匹配 Vite hash 文件名 |
| 构建版本文件 | `npm run build` 前自动生成 `frontend/public/build-info.json`，构建后复制到 `dist/build-info.json` |
| 版本日志 | 前端启动时请求 `/build-info.json`，写入 `window.__AI_BID_BUILD__` 并输出浏览器 console 日志 |
| Docker 构建注入 | `scripts/build_frontend_image.sh` 自动读取 git commit/branch/time，并通过 Docker build args 注入镜像 |

**技术实现方式**：

- `frontend/scripts/write-build-info.mjs` 读取 git commit、branch、构建时间，生成 `buildId`。
- `frontend/package.json` 增加 `prebuild`，保证本地和 Docker 镜像构建都会生成版本文件。
- `Dockerfile.frontend` 和 `docker-compose.yml` 支持 `BUILD_ID`、`BUILD_COMMIT`、`BUILD_BRANCH`、`BUILD_TIME` 构建参数；推荐用 `scripts/build_frontend_image.sh` 构建前端镜像，避免 Docker build 中缺少 `.git` 导致 commit 为 `unknown`。
- `frontend/src/utils/buildInfo.ts` 在应用启动时加载版本文件；版本信息只用于排障，不阻塞页面启动。
- `docker/nginx/app.conf` 增加 `/build-info.json` `no-store`，`/assets/` `max-age=31536000, immutable`。
- `frontend/public/build-info.json` 已加入 `.gitignore`，避免每次构建产生无意义 Git 变更。

**本地验证命令**：

```bash
cd frontend && npm run build && cd ..
scripts/build_frontend_image.sh
docker compose up -d frontend
curl -I http://127.0.0.1:8080/
curl -I http://127.0.0.1:8080/build-info.json
curl -I "http://127.0.0.1:8080/assets/<实际构建出的 js 或 css 文件名>"
```

**验收标准**：

- 上线后浏览器不会因旧 `index.html` 请求错误 API。
- 页面可看到或日志可查当前构建版本。
- `build-info.json` 可直接请求，且不会被浏览器长期缓存。

---

### P1-5 DOCX 导出页码刷新稳定化

**状态**：✅ 已完成基础版

**启动条件**：

- 不需要客户资料。
- 不需要阿里云账号即可本地开发和容器验证。
- 云上测试环境到位后，需要在 backend 容器内复验 `/usr/bin/soffice` 可用。

**已采用策略**：

- 测试版先把 LibreOffice Writer 安装进 backend 镜像，减少初期部署组件数量。
- DOCX 导出后由后端调用 `soffice --headless` 重新保存一次，用于刷新目录页码、页脚页码和总页数字段。
- 刷新失败不阻断用户下载，任务 metadata 会记录失败原因，前端会给出用户可理解的手动刷新提示。
- 后续如果并发导出量明显上升，再评估把 LibreOffice 拆成独立 worker 或 HTTP 服务。

**技术实现说明**：

| 模块 | 实现 |
| --- | --- |
| 后端导出 | `backend/export/md_to_word.py` 的 `refresh_docx_fields_with_soffice()` 统一处理 DOCX 字段刷新 |
| 任务状态 | `backend/api/export.py` 将 `field_refresh` 写入导出任务 metadata |
| 前端提示 | `frontend/src/pages/BidEditor/index.tsx` 根据 `manual_refresh_required`、`status`、`user_message` 展示成功或警告 |
| 容器环境 | `Dockerfile.backend` 使用阿里云 Debian 镜像源并安装 `libreoffice-writer`、`fontconfig`、`fonts-noto-cjk` |
| 环境变量 | 直接运行后端使用 `SOFFICE_BIN`；Docker Compose 使用 `CONTAINER_SOFFICE_BIN=/usr/bin/soffice` 映射到容器内 `SOFFICE_BIN`，避免本机 macOS 路径覆盖容器路径 |

`field_refresh` 关键字段：

| 字段 | 说明 |
| --- | --- |
| `status` | `refreshed` 表示自动刷新成功；`failed` 表示刷新失败但 DOCX 已生成；`skipped` 表示配置关闭 |
| `manual_refresh_required` | 是否需要用户在 Word/WPS 中手动刷新域 |
| `user_message` | 前端直接展示给用户的提示文案 |
| `reason` | 失败或跳过原因，便于排查 LibreOffice 路径、超时、执行失败等问题 |
| `soffice_bin` | 实际使用的 LibreOffice 可执行文件路径 |
| `timeout_seconds` | 单次刷新超时时间 |

**验收标准**：

- 导出的 DOCX 目录页码不再依赖用户第一次打开后才能刷新。
- 刷新成功时，前端提示“DOCX 已完成自动刷新”。
- 刷新失败或关闭时，下载不被阻断，前端提示用户用 Word/WPS 手动刷新域。
- 任务 metadata 有明确的 `field_refresh.status`、`field_refresh.user_message` 和失败 `reason`。
- backend 容器内可执行 `soffice --version`。

**本地验证记录（2026-05-31）**：

| 验证项 | 结果 |
| --- | --- |
| `venv/bin/python -m unittest tests.test_docx_export` | 通过，覆盖刷新成功、关闭、路径错误、超时降级 |
| `venv/bin/python -m unittest discover -s tests` | 通过，91 个测试 |
| `npm run build` | 通过；仍有 P2-7 已记录的主 chunk 偏大警告 |
| `docker compose config` | 通过 |
| `docker build --pull=false -f Dockerfile.backend -t ai-bidding-backend:local .` | 通过，backend 镜像可构建 |
| `docker compose exec -T backend printenv SOFFICE_BIN` | `/usr/bin/soffice` |
| `docker compose exec -T backend sh -lc '$SOFFICE_BIN --version'` | `LibreOffice 25.2.3.2` |
| `curl http://127.0.0.1:3012/api/ready` | `status=ok`，PostgreSQL、Redis、local storage、模型配置检查通过 |

---

### P1-6 电网分册模板扩展

**启动条件**：

- 可先用公开国网样例启动。
- 客户历史标书到位后再校准。

**工作内容**：

| 分册/章节 | 说明 |
| --- | --- |
| 技术规范书响应 | 技术偏差、参数响应、试验检验 |
| 商务响应 | 交付期、付款、质保、售后服务 |
| 资格文件 | 企业资质、人员、业绩、信用 |
| 施工/服务方案 | 安全、质量、进度、组织、应急 |
| 设备参数表 | 型号、参数、检测报告、供货清单 |

**验收标准**：

- 对电网项目生成的大纲结构明显比通用工程更贴合。
- 客户资料到位后可快速替换和校准。

---

### P1-7 前端关键流程冒烟/E2E

**启动条件**：不需要客户资料，可用公开样例和脱敏样例。

**测试主链路**：

1. 登录
2. 上传招标文件
3. 查看解析状态
4. 生成招标解读
5. 生成分册大纲
6. 生成单章正文
7. 导出 DOCX

**验收标准**：

- 每次部署后可自动或半自动完成冒烟。
- 关键 API 失败能定位到具体步骤。

---

### P1-8 阿里云 SLS 日志采集与告警

**启动条件**：

- 不需要客户业务资料。
- 需要阿里云日志服务 SLS 权限，或至少需要实施团队确认日志采集方案。

**说明**：P0-4 先完成本地结构化日志和上下文字段；本任务负责把日志接入阿里云 SLS，并配置基础查询、告警和留存策略。没有阿里云账号时，可以先设计字段和查询模板。

**工作内容**：

| 子项 | 说明 |
| --- | --- |
| SLS 采集 | 采集 backend、celery、nginx 容器 stdout |
| 日志索引 | 为 `request_id`、`module`、`stage`、`level`、`status` 建索引 |
| 查询模板 | 常用错误查询、慢请求查询、任务失败查询、模型调用失败查询 |
| 告警规则 | 5xx 激增、任务失败、OSS 上传失败、模型连续失败 |
| 留存策略 | 测试环境建议 7-30 天，生产按客户要求调整 |

**验收标准**：

- 可在 SLS 中按 `request_id` 找到一次完整请求链路。
- 任务失败和 5xx 错误有可查询模板。
- 日志采集中不出现完整密钥和客户正文全文。

---

### P1-9 向量库统一到 pgvector、移除 ChromaDB

**当前状态**：✅ 已完成（2026-05-30）。

**背景**：改造前向量存储是 pgvector 与 ChromaDB 并存的技术债：

- 知识库语义检索（读）已走 pgvector：`retrieval.py` 调用 `match_knowledge_chunks` / `match_knowledge_assets` RPC，默认配置 `vector_store=postgres_pgvector`。
- 但 `vector_store.py` 仍保留整套 ChromaDB：`file_to_chroma()` 在招标文件解析后把向量写入本地 `chroma_db/`，`query_chroma()` 仍被 `routes.py` 引用。

**改造中确认的关键事实**：经核对，ChromaDB 写入实际是死写——检索 RPC `match_knowledge_chunks` 带 `where embedding is not null` 过滤，而招标文件解析的 `document_chunks` 不写 embedding（语义入库由 `ingest_mineru_artifacts_to_supabase` 负责，向量检索数据来自知识库 `ingestion.py` 链路）；`query_chroma()` 在 `routes.py` 中仅有僵尸 import，无任何调用点（旧调用方 legacy.py 已停用）。因此移除 ChromaDB 不影响任何在用功能。

**为什么向量库选 pgvector 合理**：单企业、小团队的知识库规模（约十万到百万级 chunk）下，pgvector + HNSW/ivfflat 性能足够；统一到一个 PostgreSQL 实例后，业务数据与向量共享一套备份、监控和事务一致性（删项目时向量级联删除天然一致），运维成本最低；阿里云 RDS for PostgreSQL 原生支持 pgvector，与现有 `pgvector/pgvector:pg16` 镜像和 schema 中的向量索引一致。专用向量库的优势要到千万级以上向量和高并发检索才体现，当前阶段引入只增加运维负担。

**本次已落地产物**：

| 文件 | 说明 |
| --- | --- |
| `backend/rag/vector_store.py` | 移除 `import chromadb`、`init_chroma_client`、`file_to_chroma`、`query_chroma` 及 `__main__` 演示；保留 embedding 共享工具（`init_ali_client`/`get_embeddings`/`split_text`/`read_file_content`）；新增 `ensure_extractable_text()` 仅做“可抽取文本检测”（替代原 file_to_chroma 顺带承担的扫描件检测职责，不写任何向量库） |
| `backend/parsing/document_parser.py` | `_vectorize_markdown` 不再写 ChromaDB，仅校验产物并推进状态；native_text 兜底路径改用 `ensure_extractable_text`，保留扫描件→OCR 路由 |
| `backend/api/routes.py` | 移除僵尸 import `query_chroma` |
| `requirements.txt` | 移除 `chromadb==1.0.20` 依赖 |
| `backend/core/config.py` | 移除 `chroma_dir` 默认项和 `CHROMA_DIR` 环境变量映射 |
| `Dockerfile.backend` | 不再创建 `chroma_db` 目录 |
| `docker-compose.yml` | 移除 `app_chroma` 命名卷 |
| `tests/test_vector_store_pgvector.py` | 新增 P1-9 回归测试：ChromaDB 接口已移除、模块不再 import chromadb、`ensure_extractable_text` 空内容检测 |

**当前验证结果**：

- 从 `.venv` 卸载 `chromadb` 后，`import main` 与完整测试套件均通过，证明 ChromaDB 已彻底解耦。
- 完整测试套件 68 个用例通过（原 64 + 新增 4）。
- 检索路径回归由现有 `tests/test_rag_retrieval.py` 覆盖（pgvector RPC + rerank），召回逻辑不变。

**剩余工作**：

- 历史本地 `chroma_db/` 目录（已在 `.gitignore`）不再被任何代码读写，可由各自本地环境自行删除。
- 文档中仍有少量历史合同/设计稿提及 ChromaDB（如 `docs/清江峡能...合同草案.md`、`local_image_bid_mvp_design.md`、`notes.md`），属历史记录，不影响运行，按需在后续文档梳理时统一处理。

**验收标准**：

- 招标文件解析、知识库检索全部走 pgvector，无 ChromaDB 写入。✅
- 移除 ChromaDB 后测试套件通过，检索召回不退化。✅

---

## 7. 🟢 P2 测试后迭代增强

### P2-1 客户私有资料入库与清洗

**启动条件**：需要客户提供资料。

**资料清单**：

- 真实招标文件
- 历史中标标书
- 企业资质证书
- 人员证书
- 业绩合同/中标通知书
- 产品设备资料
- 检测报告
- 售后/运维服务资料
- 企业介绍和品牌口径

**验收标准**：

- 客户资料脱敏或权限确认后入库。
- RAG 能召回客户真实材料。
- 生成内容不编造客户没有提供的证书、人员和业绩。

---

### P2-2 国网目录编号体系

**启动条件**：

- 最好有客户历史标书或招标文件格式要求。

**说明**：如果客户明确要求 `一、（一）、1、` 这类编号体系，应提升到 P1。

**验收标准**：

- DOCX 可按环境变量切换编号风格。

---

### P2-3 AI 伴写能力

**启动条件**：不需要客户资料。

**功能范围**：

- 选中文字润色
- 扩写/缩写
- 改写为更正式
- 补充证明材料建议
- 根据资信/产品库补充事实依据

**说明**：体验加分，但不阻塞测试环境。

---

### P2-4 API 限流

**启动条件**：

- 需要 Redis，或沿用 Celery Redis。

**说明**：公网开放或多人测试时建议提前；内网小团队测试可后置。

---

### P2-5 routes.py 历史残留清理

**启动条件**：不需要客户资料。

**说明**：这是工程卫生任务。建议安排在功能冻结或上线后，不要在部署冲刺期大改路由结构。

---

### P2-6 完整 Playwright E2E 套件

**启动条件**：不需要客户资料。

**说明**：P1 先做冒烟测试，P2 再补完整 E2E 覆盖。

---

### P2-7 前端主 chunk 拆包优化

**启动条件**：不需要客户资料。

**背景**：P1-4 本地和 Docker 构建均通过，但 Vite 构建提示主 JS chunk 超过 500 kB。当前不影响测试环境运行和缓存策略，但会影响首次加载速度，后续需要优化。

**工作内容**：

- 分析 `frontend/dist/assets/index-*.js` 体积构成。
- 对 Ant Design、TipTap、Three.js、业务页面做路由级或组件级动态加载。
- 必要时在 `vite.config.ts` 增加 `manualChunks`，减少单一入口包体积。

**验收标准**：

- `npm run build` 不再出现主 chunk 超过 500 kB 的警告，或形成明确的拆包后剩余风险说明。
- 登录页、首页、标书编辑、知识库等关键页面加载正常。

---

### P2-8 前端 npm audit 高危依赖排查

**启动条件**：不需要客户资料。

**背景**：P1-4 Docker 前端镜像构建时 `npm ci` 提示 `1 high severity vulnerability`。当前未阻断测试环境构建，但上线前需要确认影响范围。

**工作内容**：

- 执行 `npm audit` 定位高危依赖来源。
- 判断是否可通过升级 patch/minor 版本解决。
- 如需 `npm audit fix`，先确认不会破坏 React/Vite/Ant Design/TipTap 兼容性。
- 如短期无法修复，输出风险说明和临时缓解措施。

**验收标准**：

- `npm audit` 无 high 级别漏洞；或已有明确的风险接受说明、影响范围和后续升级计划。

---

## 8. 推荐执行节奏

### 第 1 周：先把测试环境跑稳

| 时间 | 任务 |
| --- | --- |
| Day 1 | P0-1 阿里云资源方案、P0-4 日志体系基础版、P0-6 PostgreSQL 初始化流程 |
| Day 2-3 | P0-2 Docker 化、P0-3 gunicorn |
| Day 4 | P0-5 Nginx/HTTPS/SSE、P1-4 前端缓存策略 |
| Day 5 | P0-11 Runbook、首次本地 docker compose 冒烟 |

### 第 2 周：接入云存储和电网底座

| 时间 | 任务 |
| --- | --- |
| Day 1-2 | P0-7 OSS 最小可用 |
| Day 2-3 | P0-8 电网 Profile、P0-10 水利残留清理 |
| Day 3-4 | P0-9 电网 RAG 种子入库验证 |
| Day 5 | 阿里云测试环境首次部署 + 冒烟 |

### 第 3 周：增强稳定性和上线前质量

| 时间 | 任务 |
| --- | --- |
| Day 1-3 | P1-1 Celery 核心任务迁移 |
| Day 2-3 | P1-2 健康检查、P1-8 SLS 日志采集设计/联调 |
| Day 4 | P1-5 DOCX 页码刷新稳定化 |
| Day 5 | P1-7 冒烟/E2E、测试环境复盘 |

### 客户资料到位后

| 时间 | 任务 |
| --- | --- |
| 第 1-2 天 | P2-1 客户资料整理、脱敏、分类、入库 |
| 第 2-3 天 | P1-6 电网分册模板按客户样例校准 |
| 第 3-4 天 | 真实招标文件全链路测试 |
| 第 5 天 | 输出测试问题清单和下一轮迭代计划 |

---

## 9. 跨部门关注点

### 给老板/管理层

- 当前等待期不是空窗期，至少有 2-3 周工程底座和行业适配工作可以推进。
- 最大风险不是“客户资料没来”，而是“资料来了以后环境和行业底座没准备好”。
- 建议优先保障测试环境稳定和电网场景可信度，不要过早投入 AI 伴写等锦上添花功能。

### 给产品团队

- 需要尽快确认电网一期的默认企业画像、生成风格、分册结构和验收样例。
- 客户资料到位后，要先做资料分类和可用性判断，不要直接全量丢给研发入库。
- 需要区分“公开行业知识”和“客户私有事实”，避免 AI 混淆。

### 给开发团队

- P0-1 到 P0-13 大部分不依赖客户资料，可以立即排期。
- 优先控制部署、日志、存储、SSE、数据库初始化、行业 Prompt 这几条主线。
- 大重构任务要拆小：Celery 已先完成导出/解析/知识库入库主链路迁移；下一步只收口大纲后台精炼、连接池和最小 E2E。
- 【2026-05-30】P0-12（SQLite 下线）、P0-13（生产访问控制）、P1-3（Alembic 数据库迁移基础版）、P1-9（向量库统一到 pgvector、移除 ChromaDB）已完成；数据库与向量库统一为 PostgreSQL/pgvector 的方向已落地，后续 schema 变更应走 Alembic revision。
- 【2026-05-30】P0-6 已完成本地空库验证；P0-8/P0-10 已完成基础版，运行路径默认切换为电网/电力口径。验证命令：`venv/bin/python -m unittest discover -s tests` 通过 68 个用例，`npm run build` 通过，`docker compose config` 通过。
- 【2026-05-31】P1-1 主链路任务化完成度复核：DOCX 导出、招标解析/重试/MinerU 产物落库、知识库入库已迁 Celery；解析状态默认写 PostgreSQL `bid_parse_tasks`。复核命令：`.venv/bin/python -m unittest discover -s tests` 通过 91 个用例，`npm run build` 通过，`docker compose config --quiet` 通过。

### 给实施/运维

- 提前准备阿里云账号、资源规格、域名、证书、安全组、OSS/RDS/Redis 权限。
- `.env.production` 和密钥必须由实施管理，不进 Git。
- `.env.production` 必须按 PostgreSQL 部署配置 `DB_PROVIDER=postgres` + `DATABASE_URL`，并启用一种访问控制（推荐 `APP_LOGIN_ENABLED=true` + 强 `APP_SESSION_SECRET`），否则生产模式后端会拒绝启动。
- 每次部署后按 Runbook 做固定冒烟。

---

## 10. 首轮阿里云测试环境验收清单

- [ ] 阿里云 ECS/RDS/OSS/Redis 或本地替代资源已确认。
- [x] Docker Compose 已纳入 backend/frontend/nginx/postgres/redis/celery-worker。
- [x] Celery worker 已接入 Docker Compose。
- [x] 后端使用 gunicorn + gevent。
- [x] 日志基础设施已支持 `request_id`、`module`、`stage` 和请求耗时字段。
- [ ] 上传、解析、RAG、大纲、正文、导出等业务链路已补齐业务 ID 上下文。
- [x] 本地 Nginx 已反代 API，并配置 SSE 不缓冲。
- [ ] 云上 Nginx/HTTPS 已完成并通过外网访问验证。
- [ ] HTTPS 或测试域名访问正常。
- [x] PostgreSQL 空库初始化脚本已提供。
- [x] 初始化所需 SQL 已统一到 `migrations/postgres/`，seed 不再依赖 `sql/` 历史目录。
- [x] Alembic 迁移基础版已完成，本地版本为 `20260530_0001 (head)`。
- [x] SQLite 数据通道已下线，无静默回退本地数据库的路径。
- [x] 生产模式强制校验访问控制（登录/令牌/仅本地至少其一），否则拒绝启动。
- [x] PostgreSQL 初始化脚本已在全新 Docker 空库验证。
- [ ] PostgreSQL 初始化脚本已在阿里云 RDS 测试库验证。
- [x] pgvector 扩展可用。
- [x] OSS 上传、下载、签名 URL 基础接口可用（mock/local 已验证，真实 OSS 待账号到位后联调）。
- [x] 电网 Profile 已启用。
- [x] 关键水利默认表述已清理。
- [x] 电网 RAG 非 PDF 种子库已入库并能检索；15 份关键 PDF 标准/规范仍待 MinerU/OCR + 摘要化后补。
- [ ] 登录可用。
- [ ] 招标文件上传可用。
- [ ] 解析状态可查询。
- [ ] 招标解读可生成。
- [ ] 分册大纲可生成。
- [ ] 单章正文可生成。
- [ ] DOCX 可导出。
- [x] 生产异常和基础日志脱敏已覆盖密钥、Authorization、Cookie、DB URL 密码。
- [ ] 业务日志规则已明确禁止记录客户正文全文，并完成关键模块检查。
- [ ] 阿里云 SLS 日志采集方案和查询模板已准备；账号具备权限后可联调。
- [ ] 有部署 Runbook 和回滚说明。
