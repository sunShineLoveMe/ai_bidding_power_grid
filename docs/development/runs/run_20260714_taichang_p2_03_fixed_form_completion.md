# P2-03 固定表单与参数响应完成记录

- 日期：2026-07-14
- 真实项目：`890068b5-41fa-4f7a-bf4e-fd0169c489ec`
- 状态：完成
- 下一任务：P2-04 证据包编排与导出

## 1. 实现结果

- 直接读取 SL2655 当次招标 DOCX OOXML，语义识别商务偏差表、人员关系说明、技术偏差表、技术特性参数表，保留源文件哈希、表序号、XML 哈希、行列、列宽和横纵合并。
- 从专项技术规范两张原表提取 22 行参数要求，按产品族、内径、壁厚、参数名和检验报告证据形成 `exact_match/partial_match/mismatch/unknown` 四态响应。
- 四个真实项目章节使用确定性固定表单 renderer，模型调用为 0；偏差结论和人员关系未代选，未确认章节保持 `partial_generated`。
- 章节级 DOCX 导出检测固定表单 manifest 后，直接克隆源表 OOXML；字段刷新后执行二次审计，不通过即令导出任务失败。
- 正式检查新增 `T-013`：固定表单未确认、参数保证值未满足精确证据条件时阻断正式导出。

## 2. 当前参数覆盖结论

| 状态 | 数量 | 处理 |
| --- | ---: | --- |
| `exact_match` | 0 | 无可直接填写保证值的同规格证据 |
| `partial_match` | 10 | 仅作 `250×22` 相邻规格参考 |
| `mismatch` | 3 | 规格或数值不满足，禁止填写 |
| `unknown` | 9 | 无同名结构化证据，保持空白 |

泰昌现有 MPP 报告规格为 `DF 250×22×9000 SN40 MPP`，本项目要求内径 `200mm`、壁厚 `14mm`。环刚度 `66.40 kN/m²` 仅属于原报告规格；断裂伸长率 `176%` 还低于项目 `≥200%`。因此 22 行投标人保证值全部保持空白，不能用“完全响应”替代。

## 3. 真实项目验证

- 四章节真实 SSE：HTTP 200，均出现 `fixed_form_renderer`，模型未调用，状态 `partial_generated`。
- 浏览器：1440×960 下技术特性参数表展示 6 个原始表头和 22 行，保证值列全部空白；控制台错误 0、警告 0。
- 正式检查：63 条规则，`T-013=blocked`；证据为技术特性参数表 22 行、22 个阻断、保证值 0，未通过固定表单共 4 个。
- 真实知识问答：HTTP 200，明确回答 `250×22` 报告不能覆盖 `200×14`，不得把 `66.40/176%` 直接填入本项目保证值。

## 4. DOCX 真实导出审计

| 表单 | 源表 | 刷新后形状 | 结果 |
| --- | --- | --- | --- |
| 商务偏差表 | 主招标表 18 | 10×5 | PASS |
| 人员关系说明 | 主招标表 19 | 3×13 | PASS，双层合并表头保留 |
| 技术偏差表 | 主招标表 23 | 9×5 | PASS |
| 技术特性参数表 | 专项规范表 1、2 | 14×6 + 10×6 | PASS，22 行保证值 0 |

四份导出均使用 `template_id=current_tender_fixed_form`，`model_invoked=false`、`mineru_invoked=false`，LibreOffice 字段刷新状态 `refreshed`。刷新后二次审计确认：表数量和形状一致、合并结构一致、源单元格文字一致、列宽最大舍入差 1 twip、保证值未漂移。真实导出中曾发现相邻 `w:tbl` 被 LibreOffice 合并的问题，已通过表间 `w:p` 分隔修复并纳入回归。

## 5. 自动化与召回回归

- 后端全量：`465 passed, 11 warnings, 2 subtests passed`。
- 固定表单、Celery、DOCX、正式检查定向回归：PASS。
- 前端：`npm run build` PASS；仅保留既有 bundle 体积提示。
- Base 30：qwen3-rerank Recall@5 `96.7%`、Top1 `100%`、跨角色串扰 `0%`。
- 泰昌专项 30：qwen3-rerank Recall@5 `100%`、Top1 `100%`、禁用关键词 `0%`、跨角色串扰 `0%`。

## 6. 结论与边界

P2-03 已达到“当次招标原表保真 + 真实项目生成 + 正式导出阻断”的验收口径。当前输出是可审阅草稿，不是可提交正式标书；客户补充 `200×14` 原始报告并逐项确认保证值，同时确认偏差和人员关系结论后，方可解除 `T-013`。下一步 P2-04 只按已批准证据包映射编排附件和配图，不放宽正式资产门禁。
