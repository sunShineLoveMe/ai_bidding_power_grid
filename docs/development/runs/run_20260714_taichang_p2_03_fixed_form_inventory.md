# P2-03 固定表单与参数响应：真实源文件盘点

- 启动日期：2026-07-14
- 状态：已完成
- 真实项目：`890068b5-41fa-4f7a-bf4e-fd0169c489ec`
- 原则：以当次招标原生 DOCX 表格为格式和字段真相，不从历史标书重建固定表单，不用模型改写表头、合并单元格、声明或签章位。

## 一、主招标文件固定表单

源文件：`assets/template_words/包1_完整招标文件_92475576192439826/SL2655招标文件-预审.docx`

| 表格 | Word 原生表序号 | 原始规模 | 必须保留内容 |
| --- | ---: | --- | --- |
| 商务偏差表 | 18 | 10 行 × 5 列 | `序号/招标文件条目号/招标文件条款/投标文件条款/偏差说明`、末行投标人声明及其合并单元格 |
| 人员关系说明表 | 19 | 3 行 × 13 列 | 两级表头、13 个字段、存在/不存在两套声明选择、空白签章/确认状态 |
| 技术偏差表 | 23 | 9 行 × 5 列 | `序号/偏差事项/招标文件要求/投标文件响应/偏差说明`、`其他` 行、末行投标人声明及其合并单元格 |
| 技术特性参数表格式 | 25 | 18 行 × 5 列 | 项目抬头、`序号/名称/单位/招标人要求值/投标人保证值`、编制说明和空白响应列 |

当前 native Markdown 能识别表单位置和文字，但不能作为原表保真数据源；P2-03 必须直接读取 Word OOXML 表格、行列、合并关系和原始 XML。

## 二、专项技术规范参数原表

固化 ID 索引：

`国家电网公司总部_一级省公司固化ID修编（9111-500021520-00001）.docx`

- 1 个索引表，明确本项目为 MPP 管内径 `200mm`、壁厚 `14.0mm`、断裂延伸率 `≥200%`；投标人保证值为空。

专项技术规范：

`改性聚丙烯（MPP）电缆导管专用技术规范（内径200mm，壁厚14.0，断裂延伸率≥200%）.docx`

- 表 1：13 条产品性能参数；
- 表 2：9 条使用环境参数；
- 合计 22 条原始响应行；
- 原始字段为 `序号/参数名称/单位/项目需求值或表述/投标人保证值/备注`；
- 所有投标人保证值当前均为空，必须由证据覆盖判断和客户确认后逐项写入。

## 三、现有能力与缺口

### 可复用

- P2-01 已提供动态目录、项目规则、分标/包作用域和产品适配阻断；
- P2-02 已提供泰昌结构化参数、来源页、报告号和证据包；
- 现有偏差辅助支持数值上下限判断和 `pending_response/no_deviation/positive_deviation/negative_deviation/manual_review`；
- DOCX 导出已有正式偏差表列宽、表头、行不跨页和签章行基础能力。

### 必须新增

1. 当次招标固定表单 OOXML inventory 和原表对象模型；
2. 技术规范 22 行需求参数与泰昌 17 行 MPP 报告参数的名称、单位、规格和证据逐项映射；
3. 对外统一形成 `exact_match/partial_match/mismatch/unknown`，并保留内部偏差判断细节；
4. 规格不一致时，即使单项实测数值达到阈值，也不得自动写入“投标人保证值”；
5. 商务偏差“无/有偏差”和人员关系“存在/不存在”必须由客户确认，系统不得自动代选；
6. 原表 XML 回填、字段刷新和导出后 DOCX XML 审计。

## 四、当前项目阻断结论

泰昌现有报告为 `DF 250×22×9000 SN40 MPP`，当次招标要求为内径 `200mm`、壁厚 `14.0mm`。两者规格不一致，因此：

- 环刚度实测 `66.40 kN/m²` 虽高于项目要求 `≥24 kN/m²`，只能作相邻规格能力参考；
- 断裂伸长率实测 `176%` 低于项目要求 `≥200%`，且规格也不一致；
- 当前 22 行“投标人保证值”全部保持空白；
- 未取得 `200×14` 对应原始报告或客户逐项确认前，技术特性参数表不得进入正式投标文件；
- 不得用“完全响应”“保证满足标准”或模型生成承诺绕过阻断。

## 五、完成结果

- 已完成固定表单 OOXML inventory、22 行参数响应 manifest 和 `exact_match/partial_match/mismatch/unknown` 四态判断。
- 已接入真实项目四个章节，模型调用为 0，未确认结论保持空白并阻断正式导出。
- 已通过真实 DOCX 导出与 LibreOffice 字段刷新；刷新后表数量、行列、合并结构、列宽和源单元格文字审计 PASS。
- 已完成真实知识问答、Base 30 + 泰昌专项 30 双模式门禁、后端全量和前端构建。总记录见 `run_20260714_taichang_p2_03_fixed_form_completion.md`。
