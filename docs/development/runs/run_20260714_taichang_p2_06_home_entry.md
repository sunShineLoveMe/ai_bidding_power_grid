# P2-06 主页泰昌历史标书复用入口完成记录

- 日期：2026-07-14
- 分支：`codex/taichang-bid-reuse-v1`
- 任务：主页“泰昌历史标书复用”入口与用户流程收口
- 下一阶段：P3 真实验证与客户验收

## 实现范围

1. 在现有“智能标书”和“标书分册工作区”之间新增独立工作卡，保持左侧导航、通用上传、分册、常用功能和最近任务原结构不变。
2. 工作卡只提供“创建泰昌专版标书”一个主操作，展示“选择招标项目、确认目录与要求、匹配泰昌资料、生成技术／商务标”四个业务阶段。
3. 不展示历史模板选择、资产准备清单、基线版本、RAG、向量、内部批次或源文件路径。
4. 将通用工作卡中的“继续历史标书”改为“继续已有任务”，消除历史参考稿与历史任务的概念混淆。
5. 复用现有 `BidWorkflow`：普通入口显式请求 `general`，泰昌入口显式请求 `taichang_reuse`；选择泰昌入口后工作流显示“泰昌专版标书生成流程”，恢复和重新执行继续沿用已固化模式。

## 主要文件

- `frontend/src/components/home/TaichangReuseCard.tsx`
- `frontend/src/components/home/SmartBidCard.tsx`
- `frontend/src/pages/HomePage.tsx`
- `frontend/src/components/workflow/BidWorkflow.tsx`

## 真实浏览器验收

使用项目安装的 Playwright CLI 驱动 `http://127.0.0.1:5173/`，浏览器 API 响应采用只读拦截数据，未创建项目、未写数据库。

| 检查项 | 结果 |
| --- | --- |
| `1600×1000` 主演示视口 | PASS：入口位于指定区间，标题、说明、四步阶段和单一主按钮无错位、溢出或挤压 |
| `1280×900` 较窄视口 | PASS：`bodyScrollWidth=1280`、`rootScrollWidth=1280`、横向溢出 `false`；阶段文案正常换行 |
| 入口信息架构 | PASS：只有一个“创建泰昌专版标书”主操作，无模板选择和资产清单按钮 |
| 概念收口 | PASS：“继续已有任务”已生效；页面不展示 `V1`、批次 ID、RAG、向量或内部路径 |
| 项目模式请求 | PASS：浏览器实际 multipart 上传请求命中 `taichang_reuse=true`、`general=false` |
| 工作流反馈 | PASS：选择泰昌入口后显示“泰昌专版标书生成流程”和“泰昌专版”标签 |

浏览器截图保存在本地测试产物目录 `output/playwright/p2-06-home/`，按任务要求不纳入提交。

## 回归结果

- 前端：`npm run build` PASS；TypeScript 与 Vite 生产构建完成，仅保留既有混合 import 和大 chunk 提示。
- 后端：`.venv/bin/python -m pytest -q` → `475 passed、2 subtests passed`，11 个既有弃用告警。
- 服务：`/api/health` 为 `ok`；`/api/ready` 中数据库、Redis、Celery（1 个 worker）、模型配置和本地存储均为 `ok`。
- 项目模式：既有后端全量测试覆盖显式 `taichang_reuse` 上传、无效/关闭模式拒绝、旧项目 `general` 兼容和服务端项目模式优先。

## 门禁与边界结论

- 本轮只改变首页入口和前端工作流模式传递，没有修改 RAG 语料、metadata、召回/重排、正式资产策略、产品参数、项目业绩或 DOCX 导出规则。
- 因此本轮不触发资料重入库和 Base + 泰昌专项召回重跑；P2-05 已通过的双模式门禁继续作为当前基线，避免无数据变化时重复写入或重复生成测试产物。
- 泰昌专版仍只允许用户从专用入口主动创建；通用项目不会因企业、登录人或资料内容自动切换模式。
- 当前 SL2655 的 9 个导出前业务阻断和 1 个成品就绪阻断未被本任务绕过，正式输出仍保持草稿版。

## 结论

P2-06 已完成，P2-01～P2-06 用户可见链路闭环。下一阶段按 P3 执行真实客户项目验收；在客户补齐规格证据、报价、授权签署和正式必填字段前，不解除现有正式导出阻断。
