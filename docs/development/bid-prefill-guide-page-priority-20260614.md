# 标书前导确认页与正文导出一致性评审记录（2026-06-14）

## 结论

前导页仍有必要，但不应替代现有的章节目录编辑和正文编辑。

现有系统已经支持：

- 招标文件解析后自动生成章节目录；
- 用户自定义新增、删除、调整章节目录；
- 用户修改章节标题；
- 章节目录确认后生成正文；
- 用户在正文编辑器中修改正文。

因此，前导页的定位不应是“再做一个大表格让用户填写”，而应是生成正文前的“投标信息确认/变量收口页”。它解决的是跨章节重复变量和客户决策信息的确认问题，例如分标包号、货物清单、报价、交货期、质保期、投标保证金、授权代表、签章日期等。

## 为什么仍然需要前导页

如果没有前导页，系统会在正文生成后留下大量 `【待补充】`：

- 同一个变量会在多个章节重复出现，例如包号、投标日期、授权代理人姓名；
- 操作员需要在正文里逐处查找和修改，容易漏改；
- 后续导出 Word 时，很难判断“缺口是客户没提供”还是“已经提供但没有同步到所有章节”；
- 正文编辑适合处理章节表达，但不适合管理全局投标变量。

前导页应把这类信息在生成正文前集中收口。正文编辑器保留给用户做章节级内容调整，而不是承担全局变量管理。

## 推荐产品形态

前导页建议做成系统内的分模块确认向导，不建议直接把 649 行 CSV 给操作员填写。

| 模块 | 主要字段 | 来源 |
| --- | --- | --- |
| 项目信息 | 项目名称、招标编号、分标编号、包号、包名称 | 招标文件自动解析 + 用户确认 |
| 货物清单 | 产品名称、规格型号、数量、单位、交货地点 | 招标文件/货物清单自动解析 + 用户确认 |
| 商务报价 | 总价、分项单价、税率、大写金额 | 客户内部报价决策 |
| 投标承诺 | 交货期、质保期、投标有效期、售后响应时间 | 招标要求 + 客户承诺 |
| 保证金信息 | 金额、形式、凭证、基本账户 | 财务/商务确认 |
| 授权签章 | 授权代表、身份证号、联系方式、签署日期 | 法务/投标经办确认 |
| 项目团队 | 项目负责人、质量负责人、售后联系人 | 客户内部指派 |
| 附件确认 | 营业执照、证书、业绩、检验报告是否采用 | 企业库自动带出 + 用户确认 |

## 正文修改与 Word 导出一致性

用户在系统标书正文中修改正文后，导出的 Word 必须与用户修改后的正文一致。否则正文编辑没有产品价值。

本次已做真实回归：

- 验证记录：`docs/development/runs/run_20260614_editor_content_export_consistency.md`
- 项目 ID：`4bc3ee73-9ec5-4184-aafd-eaede9f90798`
- 链路：`build_project_bid_markdown -> convert_md_to_word -> refresh_docx_fields_with_soffice`
- 结果：PASS

验证覆盖：

1. 已保存正文：将唯一标记写入真实 `bid_sections.content`，导出 DOCX 后用 `python-docx` 读取，确认标记存在。
2. 未保存编辑器正文：通过前端同款 `sectionsSnapshot` 传入唯一标记，导出 DOCX 后确认标记存在。
3. 数据恢复：测试结束后恢复原章节正文。

结论：当前真实导出链路能保证用户修改后的正文进入 Word。后续如果改动导出任务、前端导出参数、章节保存接口，必须保留这个回归用例。

## 任务优先级

| 优先级 | 任务 | 状态 | 验收口径 |
| --- | --- | --- | --- |
| P0 | 保证正文编辑内容与 Word 导出一致 | 已验证通过 | 保存正文和未保存 `sectionsSnapshot` 两种路径都进入 DOCX |
| P0 | 前导确认页变量 schema v1 | 已完成 | P1C-4 已落地 `2026-06-16.v1`，共 32 个投标关键字段；页面为旁路只读确认，不暴露 649 行明细 |
| P0 | 变量回填引擎 | 待做 | 用户确认一次变量后，所有章节对应占位同步替换 |
| P1 | 招标文件自动预填变量 | 部分完成 | P1C-4 已从项目结构化字段、招标文本规则中生成候选值；复杂货物清单结构化联动仍需后续增强 |
| P1 | 企业库自动预填变量 | 部分完成 | P1C-4 已从企业知识/资信/产品资产生成候选附件；正式采用仍需用户确认 |
| P1 | 前导页缺口报告 | 已完成 | P1C-4 已输出客户需填写、待人工确认、正式必填缺口；真实项目接口返回正式必填缺口 15 |
| P2 | 最终排版页码回填 | 待做 | DOCX/PDF 定稿后自动或半自动回填附件页码索引 |

### 2026-06-16 P1C-4 状态同步

- 后端报告接口：`GET /api/bidding/projects/<project_id>/prefill-report`。
- 前端页面：`/prefill`，导航名称“投标确认”。
- 验收记录：`docs/rag/runs/run_20260616_p1c4_prefill_schema_gap_report_feature_summary.md`。
- 当前实现不写入 `bid_sections`，不替代正文编辑，不影响 `sectionsSnapshot` DOCX 导出契约。

## 风险与边界

- 前导页不能替代章节目录编辑；章节结构仍应允许用户自定义。
- 前导页不能替代正文编辑；正文编辑后的内容必须始终优先进入导出。
- 前导页不能让模型编造报价、保证金、授权签章、最终承诺等客户决策信息。
- 不同招标场景需要不同变量 schema，不能用一个固定 Word 模板硬套所有场景。
